---
phase: 07-french-workflow-translation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-your-shit-together/workflows/foundation-sprint-french.md
autonomous: true
requirements: [LANG-05, LANG-06]

must_haves:
  truths:
    - "foundation-sprint-french.md exists on disk after this plan with at least the file header and Steps 1–2 sections present"
    - "All Step 1 and Step 2 section name= attributes are byte-for-byte identical to the English source (no French attribute values)"
    - "The <language_directive> block appears before <objective> in the file"
    - "section_competitors_research contains a <language_reinforcement> block immediately before the section tag"
    - "write_competitors_md @-reference points to templates/fr/COMPETITORS.md (not templates/COMPETITORS.md)"
    - "section_axis_rating outputs the 7 canonical French axis labels from STATE.md plus Étroit / Large for the 8th axis"
  artifacts:
    - path: "get-your-shit-together/workflows/foundation-sprint-french.md"
      provides: "Partial French workflow covering file header through section_step2_navigation"
      contains: "language_directive"
  key_links:
    - from: "section_competitors_research"
      to: "language_reinforcement block"
      via: "placed immediately before <section name= tag"
      pattern: "<language_reinforcement>"
    - from: "write_competitors_md @-reference"
      to: "templates/fr/COMPETITORS.md"
      via: "@~/.claude/get-your-shit-together/templates/fr/COMPETITORS.md"
      pattern: "templates/fr/COMPETITORS"
---

<objective>
Translate the first half of foundation-sprint.md into foundation-sprint-french.md: file header (frontmatter, language directive, objective, onboarding, step1_banner) and all Step 1 + Step 2 sections (section_customer through section_step2_navigation — 15 of 22 sections).

Purpose: The French workflow is too large to translate in one plan without context degradation. This plan covers the deterministic, lower-complexity sections first (Steps 1 and 2), producing a concrete file on disk that Plan 02 can complete and verify.

Output: A partially-complete `get-your-shit-together/workflows/foundation-sprint-french.md` containing the language directive, objective, onboarding, step1_banner, step2_banner, and sections 1–15.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-french-output-templates/06-01-SUMMARY.md

<!-- English source to translate — read this completely before writing any French content -->
@get-your-shit-together/workflows/foundation-sprint.md

<!-- French templates — read before translating section_axis_rating to verify axis label format -->
@get-your-shit-together/templates/fr/SPRINT.md
</context>

<interfaces>
<!-- Critical locked decisions the executor must honor. Violation of any of these will break the sprint. -->

LOCKED DECISIONS (from STATE.md):

1. "vous" register — all direct address to the user uses "vous" exclusively. Never "tu", "te", "ton", "ta", "tes" in any user-facing text.

2. * MAIN ADVERSARY — the instruction in write_competitors_md that says the main adversary's heading must include `* MAIN ADVERSARY` must remain VERBATIM. Never translate this string to `* ADVERSAIRE PRINCIPAL` or any French equivalent.

3. English gyst-researcher sub-agent — the Task brief content sent to gyst-researcher in section_competitors_research stays in English. Only the surrounding prose in the French workflow is translated.

4. @./COMPETITORS.md (line ~681 in English source) — this @ reference reads the output file (not the template). It stays UNCHANGED in the French workflow.

5. Canonical French axis labels (for section_axis_rating):
   - Slow / Fast       → Lent / Rapide
   - Hard / Easy       → Difficile / Facile
   - Expensive / Free  → Cher / Gratuit
   - Complex / Simple  → Complexe / Simple
   - Dumb / Smart      → Basique / Intelligent
   - Siloed/Integrated → Cloisonné / Intégré
   - Manual/Automatic  → Manuel / Automatique
   - Narrow / Broad    → Étroit / Large  (8th axis — not locked from Phase 6, use this)

   The fr/SPRINT.md template has 7 fixed axis rows plus one `[axe personnalisé si applicable]` row.
   section_axis_rating must output these exact 8 French axis strings when recording scores.

6. XML/HTML structural elements preserved byte-for-byte:
   - <section name="..."> — name= values never translated (section_customer stays section_customer, never segment_client)
   - XML tag names: <objective>, <onboarding>, <step1_banner>, <step2_banner>, <language_directive>, <language_reinforcement> — never translated
   - YAML frontmatter field names: name:, description:, version: — field names stay in English; description: value is translated

7. @ template path replacements (for write_competitors_md only in this plan):
   - @~/.claude/get-your-shit-together/templates/COMPETITORS.md → @~/.claude/get-your-shit-together/templates/fr/COMPETITORS.md

TRANSLATION RULES:
- Translate: all prose Claude will speak to the user, all quoted questions, all banner text, all instructional comment blocks (<!-- -->), all heading text
- Preserve: name= attribute values, XML/HTML tag names, YAML field names, code fences, blank lines, horizontal rules, the literal string `* MAIN ADVERSARY`
- The `name:` YAML field value `foundation-sprint` stays as-is (it is the file identifier)
- `version: 1.0.0` stays as-is
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write French file header and Step 1 sections</name>
  <files>get-your-shit-together/workflows/foundation-sprint.md (READ ONLY — English source)
get-your-shit-together/workflows/foundation-sprint-french.md (CREATE)</files>
  <action>
Read the English source file in full. Then create `get-your-shit-together/workflows/foundation-sprint-french.md` containing the following translated blocks, in order:

1. YAML FRONTMATTER
   - name: foundation-sprint  (unchanged — file identifier)
   - description: translated to French ("Guide un entrepreneur solo à travers le Foundation Sprint — d'une idée floue à une hypothèse testable en une session.")
   - version: 1.0.0  (unchanged)

2. <language_directive> block — place immediately after the YAML frontmatter closing ---. Use this exact structure (the research document provides the canonical example):
   ```
   <language_directive>
   DIRECTIVE DE LANGUE — OBLIGATOIRE POUR TOUTE LA SESSION
   [French text instructing Claude to conduct the entire session in French. Must be unambiguous and cover: all responses in French, all questions in French, all banners/summaries in French, output files have French content, gyst-researcher operates in English which is normal, if you notice you're writing in English stop and rewrite in French.]
   Cette directive prime sur tout mode de raisonnement interne.
   </language_directive>
   ```

3. <objective> block — translate all prose. Keep XML tag name unchanged.

4. <onboarding> block — translate all prose (welcome message, step names, output file descriptions). Keep XML tag name unchanged. Keep file names (COMPETITORS.md etc.) in their original form — they are file paths, not prose.

5. <step1_banner> block — translate the banner render instructions and all quoted banner text. Keep XML tag name unchanged. The banner fields (Client, Problème, Avantages, Concurrents) use French labels. Banner values "pending" → "en attente".

6. Step 1 sections — translate in order:
   - section_customer: heading "1 of 4: Customer Segment" → "1 sur 4 : Segment client". Translate all prose, keep name= value unchanged.
   - section_problem: heading and prose. This section invokes WebSearch — add a <language_reinforcement> block BEFORE the <section name="section_problem"> tag:
     ```
     <language_reinforcement>
     Rappel de langue : vous effectuez cette validation en session française. Présentez les résultats de recherche à l'utilisateur en français.
     </language_reinforcement>
     ```
   - section_advantages: translate all prose. HIGH complexity — be thorough with the Capacity/Insight/Motivation three-loop description. Use French equivalents: Capacité, Perspicacité, Motivation.
   - section_competitors: translate all prose.
   - section_competitors_research: This section invokes the Task tool to call gyst-researcher. Add a <language_reinforcement> block BEFORE <section name="section_competitors_research">:
     ```
     <language_reinforcement>
     Rappel de langue : présentez les résultats du sous-agent à l'utilisateur en français.
     Le sous-agent gyst-researcher recherche en anglais — c'est normal et attendu. Traduisez et présentez ses conclusions en français.
     </language_reinforcement>
     ```
     The Task brief content sent to gyst-researcher (the Customer segment / Problem / User-named competitors lines) stays in English — it is an instruction to an English-only sub-agent.
   - section_main_adversary: translate all prose.
   - write_competitors_md: translate all prose. CRITICAL: Replace the @ template path → `@~/.claude/get-your-shit-together/templates/fr/COMPETITORS.md`. Do NOT change the `@./COMPETITORS.md` reference (if present — this reads the output file, not the template). The instruction about `* MAIN ADVERSARY` heading marker MUST be preserved verbatim: the string `* MAIN ADVERSARY` must appear in this section exactly as in English.
   - navigation_controls: translate all prose.

Write the complete output to `get-your-shit-together/workflows/foundation-sprint-french.md`. This is a partial file — it will be extended by Task 2. Do not add any closing markers or "end of file" indicators.
  </action>
  <verify>
    <automated>grep -c '&lt;section name=' get-your-shit-together/workflows/foundation-sprint-french.md</automated>
    Must return 10 (section_customer, section_problem, section_advantages, section_competitors, section_competitors_research, section_main_adversary, write_competitors_md, navigation_controls — that's 8 section tags; with language_reinforcement before section_problem and section_competitors_research, total section name= tags = 8).
    Also verify: grep 'language_directive' get-your-shit-together/workflows/foundation-sprint-french.md returns results.
    Also verify: grep 'templates/fr/COMPETITORS' get-your-shit-together/workflows/foundation-sprint-french.md returns results.
    Also verify: grep '\* MAIN ADVERSARY' get-your-shit-together/workflows/foundation-sprint-french.md returns results.
  </verify>
  <done>File exists. Contains language_directive block before objective. All 8 Step 1 section name= values are in English. templates/fr/COMPETITORS.md path present. * MAIN ADVERSARY string present verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Append Step 2 sections to French workflow</name>
  <files>get-your-shit-together/workflows/foundation-sprint.md (READ ONLY — English source)
get-your-shit-together/templates/fr/SPRINT.md (READ ONLY — verify axis label format)
get-your-shit-together/workflows/foundation-sprint-french.md (EXTEND)</files>
  <action>
Read the current `foundation-sprint-french.md` file content. Read the English source `foundation-sprint.md` for the Step 2 blocks. Read `templates/fr/SPRINT.md` to verify the axis label format before writing section_axis_rating.

Translate the following blocks and APPEND them to `foundation-sprint-french.md` by rewriting the file with all existing Task 1 content preserved plus the new content at the end:

1. <step2_banner> block — translate the banner render instructions and quoted banner text. Keep XML tag name unchanged. The banner shows locked axis choices (X-axis, Y-axis) — translate the field labels.

2. Step 2 sections — translate in order:
   - section_axis_rating: This is the CANONICAL AXIS section. Translate heading and all prose. The 8 axes listed in the English source must appear in French using EXACTLY these strings:
     1. Lent ←——————→ Rapide
     2. Difficile ←——————→ Facile
     3. Cher ←——————→ Gratuit
     4. Complexe ←——————→ Simple
     5. Basique ←——————→ Intelligent
     6. Cloisonné ←——————→ Intégré
     7. Manuel ←——————→ Automatique
     8. Étroit ←——————→ Large
     The confirmation output block (where scores are displayed after user input) must also use these exact French labels.
   - section_custom_axes: translate all prose.
   - section_axis_selection: translate all prose. List of all rated axes with scores must use French axis names.
   - section_competitor_scoring: translate all prose.
   - section_matrix_render: translate all prose (render instruction, 2x2 ASCII matrix labels if any are prose).
   - section_manifesto: HIGH complexity. Translate all prose including the mini-manifesto examples and evaluation criteria thoroughly. Use "vous" register.
   - section_step2_navigation: translate all prose including navigation options.

Write the updated complete file (Task 1 content + Task 2 content) to `foundation-sprint-french.md`.
  </action>
  <verify>
    <automated>grep -c '&lt;section name=' get-your-shit-together/workflows/foundation-sprint-french.md</automated>
    Must return 15 (8 from Task 1 + 7 from Task 2: section_axis_rating, section_custom_axes, section_axis_selection, section_competitor_scoring, section_matrix_render, section_manifesto, section_step2_navigation).
    Also verify axis labels: grep 'Lent' get-your-shit-together/workflows/foundation-sprint-french.md returns results.
    Also verify: grep 'Étroit' get-your-shit-together/workflows/foundation-sprint-french.md returns results.
  </verify>
  <done>File contains 15 section name= tags (Steps 1 and 2 complete). French canonical axis labels present verbatim. section_manifesto content is fully translated prose (not stub or placeholder).</done>
</task>

</tasks>

<verification>
After both tasks complete:
- foundation-sprint-french.md exists and is non-empty
- grep -c '<section name=' foundation-sprint-french.md returns 15
- grep 'language_directive' foundation-sprint-french.md returns results
- grep 'MAIN ADVERSARY' foundation-sprint-french.md returns results (verbatim marker preserved)
- grep 'templates/fr/COMPETITORS' foundation-sprint-french.md returns results (path replaced)
- grep 'Lent' foundation-sprint-french.md returns results (axis labels canonical)
- No 'tu\b' matches in user-facing prose (vous register maintained)
</verification>

<success_criteria>
foundation-sprint-french.md exists covering the file header and all 15 of 22 sections (Steps 1 and 2). The language_directive block is present at the top. All section name= values are English identifiers. Canonical French axis labels are used in section_axis_rating. The * MAIN ADVERSARY marker and @./COMPETITORS.md output reference are verbatim. templates/fr/COMPETITORS.md @ path is used in write_competitors_md.
</success_criteria>

<output>
After completion, create `.planning/phases/07-french-workflow-translation/07-01-SUMMARY.md`
</output>
