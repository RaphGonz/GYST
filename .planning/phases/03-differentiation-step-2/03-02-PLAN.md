---
phase: 03-differentiation-step-2
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - get-your-shit-together/workflows/foundation-sprint.md
autonomous: true
requirements: [SPRINT-09, SPRINT-10, SPRINT-11, RESEARCH-02]

must_haves:
  truths:
    - "After axes are locked, AI reads COMPETITORS.md using the Read tool and derives scores for each competitor on both axes — no web searches occur"
    - "Each competitor's scores are displayed with a 1-line rationale before the matrix is rendered"
    - "The 2x2 ASCII matrix shows all competitors placed spatially in their correct quadrant; 'You' is always in the top-right"
    - "If any competitor has both X-score > 0 and Y-score > 0, AI hard-blocks with a conflict message and returns to axis selection — no 'continue anyway' option"
    - "When no conflict exists, user writes all 3 manifesto phrases at once; AI gives one holistic evaluation (not phrase-by-phrase)"
    - "A Step 2 summary block and A/B navigation menu appear after the manifesto is locked"
    - "Choosing A advances to Step 3 stub; choosing B asks what to revisit (axis selection or manifesto only — no full step wipe)"
  artifacts:
    - path: "get-your-shit-together/workflows/foundation-sprint.md"
      provides: "Complete Step 2 flow including competitor scoring, matrix, conflict detection, manifesto, summary, navigation, and step3_stub"
      contains: "section_competitor_scoring, section_matrix_render, section_manifesto, section_step2_navigation, step3_stub"
  key_links:
    - from: "section_axis_selection (Plan 03-01)"
      to: "section_competitor_scoring"
      via: "direct flow after axis lock"
      pattern: "section_competitor_scoring"
    - from: "section_competitor_scoring"
      to: "COMPETITORS.md"
      via: "@./COMPETITORS.md Read tool reference — no WebSearch"
      pattern: "@./COMPETITORS.md"
    - from: "section_matrix_render"
      to: "section_conflict_check"
      via: "inline conflict detection after matrix output"
      pattern: "section_conflict_check|conflict"
    - from: "section_manifesto"
      to: "section_step2_navigation"
      via: "flow after manifesto locked"
      pattern: "section_step2_navigation"
---

<objective>
Append the second half of Step 2 to foundation-sprint.md: competitor scoring from COMPETITORS.md profiles (no web search), ASCII 2x2 matrix rendering, conflict detection with hard block, mini-manifesto elicitation with holistic evaluation, and Step 2 summary + navigation controls (with step3_stub).

Purpose: This is the core of the Differentiation phase — taking the axes locked in Plan 03-01 and converting them into a visual positioning map, then capturing the mini-manifesto before advancing. RESEARCH-02 is an absolute constraint throughout: no web searching.

Output: foundation-sprint.md complete through Step 2. Five new named sections appended: section_competitor_scoring, section_matrix_render (with inline conflict check), section_manifesto, section_step2_navigation, and step3_stub. A user can run the entire sprint through Step 2 end-to-end.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-differentiation-step-2/03-CONTEXT.md
@.planning/phases/03-differentiation-step-2/03-RESEARCH.md
@.planning/phases/03-differentiation-step-2/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Key interfaces the executor needs. -->

From Plan 03-01 output (sections that must already exist in the file before this plan runs):
- step2_banner — banner re-render block
- section_axis_rating — collects and locks 8 axis scores
- section_custom_axes — optional custom axes
- section_axis_selection — user picks 2 differentiating axes, locks banner, then flows to section_competitor_scoring

COMPETITORS.md runtime structure (template in get-your-shit-together/templates/COMPETITORS.md):
```
### [Competitor Name] * MAIN ADVERSARY   ← may or may not have this marker
**Type:** Direct / Indirect
**What they do:** [description]
**Pricing model:** [model]
**Known strengths:** [bullets]
**Known weaknesses:** [bullets]
**Positioning signals:** [claims — PRIMARY signal source for axis scoring]
**Research sources:** [URLs]
```

Scoring rubric from RESEARCH.md (the workflow section must embed this):
| Axis | Profile field(s) to use |
|------|------------------------|
| Slow ←→ Fast | Onboarding description, time-to-value claims in "What they do" |
| Hard ←→ Easy | Setup friction, target audience signals in strengths/weaknesses |
| Expensive ←→ Free | "Pricing model" directly |
| Complex ←→ Simple | Feature count signals; "all-in-one" = complex, "focused" = simple |
| Dumb ←→ Smart | AI/automation mentions in strengths or positioning signals |
| Siloed ←→ Integrated | Integration/API mentions in strengths or positioning signals |
| Manual ←→ Automatic | Automation features in strengths or "what they do" |
| Narrow ←→ Broad | Target audience breadth in "what they do" or positioning signals |
| Domain-specific axes | Positioning signals field |

Conflict condition: x_score > 0 AND y_score > 0 for any competitor = hard block.
Score of 0 = center line; negative = left/bottom half.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add section_competitor_scoring and section_matrix_render (with conflict check)</name>
  <files>get-your-shit-together/workflows/foundation-sprint.md</files>
  <action>
Read get-your-shit-together/workflows/foundation-sprint.md (already extended through section_axis_selection from Plan 03-01). Append the following two sections at the end of the file:

```markdown
<section name="section_competitor_scoring">

## Competitor Scoring (RESEARCH-02)

**When entering this section:** After the 2 differentiating axes are locked and the Step 2 banner has been re-rendered.

**CRITICAL: Do NOT run any web searches in this section. Do NOT call WebSearch or WebFetch. There is NO exception to this rule. All scoring uses ONLY information already in COMPETITORS.md. If a profile field is missing or empty, score 0 and flag as "limited data".**

Read the competitor profiles now:

@./COMPETITORS.md

For each competitor in COMPETITORS.md, derive a score from -5 to +5 on each of the 2 selected axes (X-axis and Y-axis locked in section_axis_selection).

Use ONLY these profile fields as evidence:
- **For price-related axes (Expensive ←→ Free):** Use the "Pricing model" field directly.
- **For speed-related axes (Slow ←→ Fast):** Look for time-to-value claims and onboarding descriptions in "What they do."
- **For ease-related axes (Hard ←→ Easy):** Look for setup friction, technical audience signals in "Known strengths" and "Known weaknesses."
- **For complexity axes (Complex ←→ Simple):** Count feature breadth signals; "all-in-one" or "comprehensive" = more complex; "focused" or "single-purpose" = simpler.
- **For intelligence axes (Dumb ←→ Smart):** Look for AI, automation, or intelligence claims in "Known strengths" and "Positioning signals."
- **For integration axes (Siloed ←→ Integrated):** Look for API, integration ecosystem, or "connects with" mentions in "Known strengths" and "Positioning signals."
- **For automation axes (Manual ←→ Automatic):** Look for workflow automation claims in "Known strengths" and "What they do."
- **For scope axes (Narrow ←→ Broad):** Look for vertical/horizontal claims and target audience breadth in "What they do" and "Positioning signals."
- **For domain-specific custom axes:** Use the "Positioning signals" field as the primary signal source.

If a required field is empty or "Unknown": score 0 and flag it explicitly.

Do NOT infer from general market knowledge. Do NOT search the web. Score 0 if you cannot support a score from the profile text.

Display scores with rationale BEFORE rendering the matrix:

> Scoring competitors on [X-axis] and [Y-axis]:
>
> [CompA]: X-axis [score], Y-axis [score] — [one key detail from their profile that drove this rating]
> [CompB]: X-axis [score], Y-axis [score] — [key detail]; [axis] 0 — limited data ([field name] not found)
> (repeat for each competitor)

After displaying all scores: proceed to section_matrix_render.

</section>

<section name="section_matrix_render">

## 2x2 Matrix and Conflict Check (SPRINT-09, SPRINT-10)

**When entering this section:** After all competitor scores are displayed.

**Step 1: Assign quadrants.**

For each competitor:
- X-score > 0 → right half. X-score ≤ 0 → left half.
- Y-score > 0 → top half. Y-score ≤ 0 → bottom half.
- Score of exactly 0 → place near the center line of that axis.

"You" (dream company) is ALWAYS placed in top-right, regardless of scores.

**Step 2: Render the ASCII grid.**

Grid format (approximately 60 characters wide):

```
              High [Y-axis right pole]
                        ^
  [top-left names]      |      You
                        |  [top-right names]
  ────────────────────────────────────────►
                        |   High [X-axis right pole]
  [bottom-left names]   |
                        |  [bottom-right names]
              Low [Y-axis left pole]
```

Rules:
- Place competitor names spatially within their quadrant region — names only, no coordinates.
- Truncate names longer than 15 characters with "..." (e.g., "CompetitorName..." → "CompetitorNam...")
- If multiple competitors land in the same quadrant: stack them vertically (one per line).
- If a quadrant is empty: render "—" in that quadrant region.
- The grid must always show all 4 quadrants even if some are empty.
- "You" appears in the top-right area of the grid.
- Label axes outside the grid: top = High [Y right pole], bottom = Low [Y left pole], right = High [X right pole].

**Step 3: Render the rationale block below the grid.**

After the grid, show one line per competitor:

> Competitor positions:
> - [CompA] (top-right): [key profile detail that drove placement] — CONFLICT
> - [CompB] (bottom-left): [key profile detail]
> - [CompC] (bottom-right): [key profile detail]
> (Note conflicts in rationale with "— CONFLICT" marker)

**Step 4: Conflict check (SPRINT-10).**

After the matrix and rationale block are rendered:

Check: Does any competitor have BOTH x_score > 0 AND y_score > 0?

**If YES (conflict):**

Display immediately after the rationale block:

> **CONFLICT DETECTED**
>
> [CompA] lands in the top-right quadrant — the same position as your dream company.
>
> This means [CompA] already holds the differentiating position you're claiming.
> Customers who see both of you won't have a clear reason to choose you over them.
>
> You need to choose 2 different differentiating axes — ones where YOU are in the
> top-right and [CompA] is not. Your ratings on all 8+ axes are preserved.

**There is NO option to continue with a conflict. Do NOT say "you could proceed anyway." Do NOT offer any alternative path. The ONLY available action is axis re-selection.**

After the conflict message: ask the user to pick 2 new differentiating axes. Return to section_axis_selection. Start the selection process from the beginning of that section.

**If NO conflict:** Proceed directly to section_manifesto.

</section>
```

After writing, verify both sections are present and the file contains "@./COMPETITORS.md" as the profile read instruction.
  </action>
  <verify>
    <automated>grep -q "section_competitor_scoring" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "section_matrix_render" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "@./COMPETITORS.md" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "CONFLICT DETECTED" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "Do NOT run any web searches" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    section_competitor_scoring and section_matrix_render are present. The file contains "@./COMPETITORS.md" as the read reference. "Do NOT run any web searches" appears in the scoring section. "CONFLICT DETECTED" appears in the conflict check block. The conflict block explicitly states no "continue anyway" option.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add section_manifesto, section_step2_navigation, and step3_stub</name>
  <files>get-your-shit-together/workflows/foundation-sprint.md</files>
  <action>
Read get-your-shit-together/workflows/foundation-sprint.md (already extended through section_matrix_render from Task 1). Append the following three sections at the end of the file:

```markdown
<section name="section_manifesto">

## Mini-Manifesto (SPRINT-11)

**When entering this section:** After the 2x2 matrix is confirmed with no conflict.

Say:

> "Now write your mini-manifesto — 3 short phrases that define what you stand for.
>
> Write them as advice to a new team member, not as marketing copy.
> They should constrain decisions, not describe aspirations.
>
> **Phrase 1 (Differentiator 1):** Connected to your X-axis position
>    Example: "We are fully automated — no manual steps for the customer, ever."
>
> **Phrase 2 (Differentiator 2):** Connected to your Y-axis position
>    Example: "We are always free to start — no credit card, no trial expiry."
>
> **Phrase 3 (Safeguard):** What you will never compromise on, even if it costs you
>    Example: "We will never add enterprise features that break the simple user experience."
>
> Write all three at once."

Wait for user response.

**Evaluate all 3 phrases together in ONE holistic response — do NOT critique each phrase separately.**

Evaluation criteria (check all three together):
- Do they read as decision-making tools, not marketing headlines?
- Are they specific enough to actually constrain a product decision?
- Do they connect to the chosen differentiating axes?

Invalid examples (marketing copy — reject these):
- "We are the leader in X" / "We deliver unparalleled Y" / "We are the fastest/best/cheapest"

Valid examples (decision-making constraints):
- "We build for one persona and say no to feature requests from others"
- "We never charge per seat — pricing is always flat"
- "We will never add features that require a sales call to explain"

**If strong:** Say "These work. Locking your manifesto." Then lock all 3 phrases.

**If marketing copy or too vague:** Give ONE round of feedback — explain what a decision-constraining version looks like and give a specific rewrite example. Then accept whatever the user writes next without further pushback.

After locking: re-render the Step 2 banner with "Manifesto: locked". Then proceed to section_step2_navigation.

</section>

<section name="section_step2_navigation">

## Step 2 Summary and Navigation

**When entering this section:** After the manifesto is locked.

Display the Step 2 complete summary block:

─── Step 2 Complete ─────────────────────────────
Differentiating axes:
  X: [axis name] — You: [score]
  Y: [axis name] — You: [score]

Competitor positions:
  [CompA]: X: [score], Y: [score] → [quadrant]
  [CompB]: X: [score], Y: [score] → [quadrant]
  (all competitors from COMPETITORS.md)

Mini-manifesto:
  [Phrase 1]
  [Phrase 2]
  [Phrase 3]
─────────────────────────────────────────────────

Then ask:

> "What would you like to do?
>
> **A) Continue to Step 3** — solution approaches
> **B) Go back** — revisit axis selection or manifesto"

Wait for user response.

**If A:** Proceed to step3_stub.

**If B:** Ask what they want to revisit:

> "What do you want to go back to?
>
> **1) Axis selection** — pick different X and Y axes (your ratings on all 8+ axes are preserved)
> **2) Manifesto** — rewrite your mini-manifesto (axes and matrix are preserved)"

Wait for user choice.
- If "1" or "axis selection": return to section_axis_selection. All axis ratings are preserved — only the choice of which 2 to use as differentiators is redone.
- If "2" or "manifesto": return to section_manifesto. Axes, matrix, and scores are preserved.

Do NOT offer to wipe all of Step 2. Do NOT offer to restart Step 1. Targeted redo only.

</section>

<step3_stub>
<!-- STEP 3 STUB: Phase 4 fills in Approaches logic -->

After Step 2 navigation confirms "advance to Step 3", tell the user:

> "Step 3 (Approaches) is not yet implemented in this version. Your Step 2 decisions have been captured in this session.
>
> **Your Step 2 summary:**
> - X-axis: [locked X-axis and your score]
> - Y-axis: [locked Y-axis and your score]
> - Manifesto: [your 3 phrases]
>
> Stay tuned for the next release."

Do not attempt to run Step 3 logic. The sprint ends here for now.

</step3_stub>
```

After writing, verify all three sections are present. Run `node bin/install.js` to push changes to ~/.claude/get-your-shit-together/. Note: run from the GYST repo root (C:/Users/raphg/Desktop/GYST), not from inside the get-your-shit-together directory.
  </action>
  <verify>
    <automated>grep -q "section_manifesto" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "section_step2_navigation" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "step3_stub" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "DO NOT critique each phrase separately\|do NOT critique each phrase separately\|not critique each phrase separately" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && wc -l "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" | awk '{if ($1 > 700) print "PASS"; else print "FAIL: file too short, got " $1}'</automated>
  </verify>
  <done>
    section_manifesto, section_step2_navigation, and step3_stub are all present. The manifesto section explicitly prohibits phrase-by-phrase critique. The navigation section offers targeted redo (axis selection or manifesto) without full step wipe. File is longer than 700 lines. install.js has been run and changes are live in ~/.claude/get-your-shit-together/workflows/foundation-sprint.md.
  </done>
</task>

</tasks>

<verification>
After both tasks, the complete Step 2 flow is in place. Verify the full sequence:

1. `grep -c "section_competitor_scoring\|section_matrix_render\|section_manifesto\|section_step2_navigation\|step3_stub" get-your-shit-together/workflows/foundation-sprint.md` returns 5
2. `grep -c "WebSearch\|WebFetch" get-your-shit-together/workflows/foundation-sprint.md` returns 0 (no web search calls anywhere in the file)
3. `grep -q "CONFLICT DETECTED" get-your-shit-together/workflows/foundation-sprint.md` passes
4. `grep -q "@./COMPETITORS.md" get-your-shit-together/workflows/foundation-sprint.md` passes (Read tool reference present)
5. `grep -q "Do NOT run any web searches" get-your-shit-together/workflows/foundation-sprint.md` passes
6. `grep -q "step3_stub" get-your-shit-together/workflows/foundation-sprint.md` passes (graceful dead-end present)
7. File line count greater than 700
8. Install script ran successfully — ~/.claude/get-your-shit-together/workflows/foundation-sprint.md reflects the changes
</verification>

<success_criteria>
- User can rate axes (Plan 03-01) and have competitor scoring run automatically from COMPETITORS.md profiles — no web search
- The ASCII 2x2 matrix renders with all competitors placed in their correct quadrants
- If any competitor is in the top-right quadrant, the sprint hard-blocks and forces axis re-selection
- If no conflict, user writes 3 manifesto phrases and receives holistic (not phrase-by-phrase) feedback
- After manifesto is locked, a structured summary block appears followed by A (advance) or B (go back)
- Go-back offers targeted redo of axis selection or manifesto — no full Step 2 wipe
- Step 3 stub gracefully ends the session with a summary of Step 2 decisions
- All RESEARCH-02 constraints upheld: no web searching in any Step 2 section
</success_criteria>

<output>
After completion, create `.planning/phases/03-differentiation-step-2/03-02-SUMMARY.md` following the summary template at @C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</output>
