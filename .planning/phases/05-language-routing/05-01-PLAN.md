---
phase: 05-language-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gyst/foundation-sprint.md
  - ~/.claude/commands/gyst/foundation-sprint.md
autonomous: false
requirements:
  - LANG-01
  - LANG-02
  - LANG-03

must_haves:
  truths:
    - "Running /gyst:foundation-sprint with no argument loads the English workflow — identical behavior to v1.0"
    - "Running /gyst:foundation-sprint with -french causes Claude to acknowledge the flag and attempt to read foundation-sprint-french.md (graceful failure is acceptable; silent English fallback is not)"
    - "Running /gyst:foundation-sprint with an unrecognized flag (e.g. -spanish) prints a message naming the flag, states English will be used, then loads the English workflow"
    - "The command file contains no <execution_context> block — no static @-include that would override routing"
    - "The argument-hint field shows [-french] in autocomplete"
    - "Both the repo copy and the installed copy are identical after deployment"
  artifacts:
    - path: "commands/gyst/foundation-sprint.md"
      provides: "Source-of-truth command file with routing <process> block"
      contains: "$ARGUMENTS"
    - path: "~/.claude/commands/gyst/foundation-sprint.md"
      provides: "Deployed command file Claude Code reads at runtime"
      contains: "$ARGUMENTS"
  key_links:
    - from: "commands/gyst/foundation-sprint.md"
      to: "~/.claude/get-your-shit-together/workflows/foundation-sprint.md"
      via: "Read tool call triggered by <process> routing (no-flag branch)"
      pattern: "foundation-sprint\\.md"
    - from: "commands/gyst/foundation-sprint.md"
      to: "~/.claude/get-your-shit-together/workflows/foundation-sprint-french.md"
      via: "Read tool call triggered by <process> routing (-french branch)"
      pattern: "foundation-sprint-french\\.md"
---

<objective>
Update the command file at `commands/gyst/foundation-sprint.md` to replace the static `<execution_context>` block with a three-branch routing `<process>` block driven by `$ARGUMENTS`, then deploy the updated file to the installed location.

Purpose: Enables language selection at invocation time — the architectural foundation for all future language routing. Without this change, any flag passed by the user is silently ignored and the English workflow always runs.

Output: Two identical updated command files (repo + installed), with routing logic live and verifiable.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-language-routing/05-RESEARCH.md

<interfaces>
<!-- Current command file structure — executor needs to know what is being replaced -->
<!-- Source: commands/gyst/foundation-sprint.md, read 2026-03-08 -->

Current file (verbatim, showing what changes):

```markdown
---
name: gyst:foundation-sprint
description: Run a Foundation Sprint — one session, one testable hypothesis
allowed-tools:
  - Read
  - Write
  - Bash
  - WebSearch
  - WebFetch
  - Task
argument-hint: (no arguments needed — just run it)   ← REPLACE with "[-french]"
---

<objective>
Run the GYST Foundation Sprint end-to-end in a single session. You are a thinking partner guiding a solo entrepreneur from a fuzzy idea to a testable hypothesis using the Foundation Sprint method.

This sprint produces four output files in the user's current directory:
- COMPETITORS.md — fixed competitor list with research profiles (written after Step 1)
- HYPOTHESIS.md — the full X/Y/Z/W/U/V hypothesis + testable form (written at sprint end)
- SPRINT.md — complete journal of every decision made at each step (written at sprint end)
- POSITIONING.md — 2x2 matrix, mini-manifesto, differentiation axes (written at sprint end)
</objective>

<execution_context>                                   ← DELETE this block entirely
@~/.claude/get-your-shit-together/workflows/foundation-sprint.md
</execution_context>

<process>
Execute the foundation-sprint workflow from @~/.claude/...  ← REPLACE entire block
</process>
```

Target `<process>` block (exact content to write):

```markdown
<process>
Before executing any workflow, check $ARGUMENTS:

- If $ARGUMENTS contains "-french": read and execute
  ~/.claude/get-your-shit-together/workflows/foundation-sprint-french.md
- If $ARGUMENTS is empty or contains no language flag: read and execute
  ~/.claude/get-your-shit-together/workflows/foundation-sprint.md
- If $ARGUMENTS contains an unrecognized flag: say "Language '$ARGUMENTS' is
  not yet supported. Running the sprint in English." then execute
  ~/.claude/get-your-shit-together/workflows/foundation-sprint.md

Follow all instructions in the selected workflow precisely.
</process>
```

DO NOT use @-syntax inside the `<process>` block — plain paths only. @-includes are resolved statically before Claude reads `<process>`, which would defeat routing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite command file with routing logic</name>
  <files>commands/gyst/foundation-sprint.md</files>
  <action>
Rewrite `commands/gyst/foundation-sprint.md` with the following exact changes (all other content is preserved verbatim):

1. Change `argument-hint` from:
   `argument-hint: (no arguments needed — just run it)`
   to:
   `argument-hint: "[-french]"`

2. Delete the entire `<execution_context>` block (lines 24-26 in current file):
   ```
   <execution_context>
   @~/.claude/get-your-shit-together/workflows/foundation-sprint.md
   </execution_context>
   ```
   Remove the blank line after it too — no orphan whitespace.

3. Replace the entire `<process>` block with:
   ```
   <process>
   Before executing any workflow, check $ARGUMENTS:

   - If $ARGUMENTS contains "-french": read and execute
     ~/.claude/get-your-shit-together/workflows/foundation-sprint-french.md
   - If $ARGUMENTS is empty or contains no language flag: read and execute
     ~/.claude/get-your-shit-together/workflows/foundation-sprint.md
   - If $ARGUMENTS contains an unrecognized flag: say "Language '$ARGUMENTS' is
     not yet supported. Running the sprint in English." then execute
     ~/.claude/get-your-shit-together/workflows/foundation-sprint.md

   Follow all instructions in the selected workflow precisely.
   </process>
   ```
   Use plain paths — NOT @-syntax — inside `<process>`. @-includes are static injections; plain paths are read dynamically by the Read tool at Claude's discretion.

The `<objective>` block and all frontmatter fields other than `argument-hint` are unchanged. The final file has no `<execution_context>` block at all.
  </action>
  <verify>
    <automated>grep -c "execution_context" C:/Users/raphg/Desktop/GYST/commands/gyst/foundation-sprint.md; grep "argument-hint" C:/Users/raphg/Desktop/GYST/commands/gyst/foundation-sprint.md; grep "ARGUMENTS" C:/Users/raphg/Desktop/GYST/commands/gyst/foundation-sprint.md</automated>
  </verify>
  <done>
- `grep -c "execution_context" commands/gyst/foundation-sprint.md` returns 0
- `grep "argument-hint"` shows `argument-hint: "[-french]"`
- `grep "ARGUMENTS"` shows at least one line containing $ARGUMENTS in the process block
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy updated command file to installed location</name>
  <files>~/.claude/commands/gyst/foundation-sprint.md</files>
  <action>
Copy the updated repo command file to the installed location so Claude Code reads the new routing logic:

```bash
cp C:/Users/raphg/Desktop/GYST/commands/gyst/foundation-sprint.md ~/.claude/commands/gyst/foundation-sprint.md
```

Then verify the two copies are identical (no diff):

```bash
diff C:/Users/raphg/Desktop/GYST/commands/gyst/foundation-sprint.md ~/.claude/commands/gyst/foundation-sprint.md
```

If diff output is non-empty, something went wrong with the copy — re-copy before proceeding.

Do not run `node bin/install.js` unless the cp command is unavailable — the installer also copies workflows and templates, which are unchanged in Phase 5 and safe to re-copy, but cp is faster and more surgical.
  </action>
  <verify>
    <automated>diff C:/Users/raphg/Desktop/GYST/commands/gyst/foundation-sprint.md ~/.claude/commands/gyst/foundation-sprint.md && echo "IDENTICAL" || echo "MISMATCH"</automated>
  </verify>
  <done>
`diff` produces no output and exits 0. Both files contain the routing `<process>` block and no `<execution_context>` block.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all three routing branches in Claude Code</name>
  <files>~/.claude/commands/gyst/foundation-sprint.md</files>
  <action>
Open Claude Code and test all three routing branches using the deployed command file. This cannot be automated — routing behavior requires a live Claude Code session to observe.
  </action>
  <what-built>Updated command file with $ARGUMENTS-driven routing, deployed to ~/.claude/commands/gyst/foundation-sprint.md. No static @-include. Three routing branches: no-flag → English, -french → French (expected to fail gracefully), unknown flag → message + English.</what-built>
  <how-to-verify>
Open a fresh Claude Code session for each test. Test all three branches:

**Branch 1 — No flag (English, unchanged behavior):**
1. Run `/gyst:foundation-sprint` with no arguments
2. Expected: Claude begins the English Foundation Sprint exactly as before (Step 1 intro, asks for your business idea)
3. Pass if: Sprint starts normally in English
4. Fail if: Claude acknowledges a flag, prints an error, or behaves differently from v1.0

**Branch 2 — French flag (routing live, graceful failure acceptable):**
1. Run `/gyst:foundation-sprint` with argument `-french`
2. Expected: Claude acknowledges the -french flag, attempts to read `~/.claude/get-your-shit-together/workflows/foundation-sprint-french.md`, and reports the file does not exist (or similar error)
3. Pass if: Claude's output references `foundation-sprint-french.md` before failing
4. Fail if: Claude silently starts the English sprint without acknowledging the flag at all

**Branch 3 — Unknown flag (message + English fallback):**
1. Run `/gyst:foundation-sprint` with argument `-spanish`
2. Expected: Claude prints a message containing "-spanish" (or "spanish"), states English will be used, then starts the English Foundation Sprint
3. Pass if: Message is shown AND English sprint starts
4. Fail if: Silent English fallback with no message, OR sprint errors out entirely

**Autocomplete check (optional but quick):**
- Type `/gyst:foundation-sprint` in Claude Code and check that the autocomplete hint shows `[-french]`
  </how-to-verify>
  <verify>
    <automated>MISSING — routing behavior requires a live Claude Code session; no automated check exists for Claude's runtime response to slash command invocation</automated>
  </verify>
  <done>All three routing branches behave as specified: no-flag starts English, -french attempts French file load and acknowledges the flag, unrecognized flag prints a message and starts English.</done>
  <resume-signal>Type "approved" if all three branches pass, or describe which branch failed and what happened instead</resume-signal>
</task>

</tasks>

<verification>
All three routing branches verified by human in Claude Code:
1. No-flag invocation starts English sprint (LANG-01 backward compat, LANG-02)
2. -french flag causes Claude to attempt French workflow load and reference the correct path (LANG-01)
3. Unknown flag triggers informative message then English fallback (LANG-03)

File-level: both copies of the command file are diff-clean and contain no `<execution_context>` block.
</verification>

<success_criteria>
- `grep -c "execution_context" commands/gyst/foundation-sprint.md` returns 0
- `grep "argument-hint"` returns `argument-hint: "[-french]"`
- `diff` of both file copies returns exit 0 (no differences)
- Human confirms all three routing branches pass in Claude Code
- LANG-01, LANG-02, LANG-03 marked verifiable (French file absent by design until Phase 7)
</success_criteria>

<output>
After completion, create `.planning/phases/05-language-routing/05-01-SUMMARY.md` following the summary template at `@C:/Users/raphg/.claude/get-shit-done/templates/summary.md`.

Key fields to include:
- decisions: LANG-02 interpretation — the unrecognized-flag fallback satisfies "zero command file changes for future languages"; adding an explicit routing bullet per language is a one-bullet additive change, not required for the fallback to work
- patterns: routing via natural language `<process>` block with `$ARGUMENTS`; two-copy deployment (repo → cp → ~/.claude)
- concerns: Phase 7 must confirm `-french` branch successfully loads `foundation-sprint-french.md` end-to-end once the file exists
</output>
