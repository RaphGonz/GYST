---
phase: 01-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bin/install.js
  - commands/gyst/foundation-sprint.md
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "Running `node bin/install.js` without errors copies files to ~/.claude/get-your-shit-together/ and ~/.claude/commands/gyst/"
    - "The slash command file exists at commands/gyst/foundation-sprint.md with correct frontmatter and @-reference to the workflow"
    - "package.json declares bin pointing to bin/install.js with the correct files array"
    - "bin/install.js is executable as a Node.js script (shebang present, no external dependencies)"
  artifacts:
    - path: "package.json"
      provides: "npm package definition with bin field and files array"
      contains: "\"bin\": { \"get-your-shit-together\": \"bin/install.js\" }"
    - path: "bin/install.js"
      provides: "Interactive installer that copies files to ~/.claude/"
      min_lines: 60
    - path: "commands/gyst/foundation-sprint.md"
      provides: "Slash command wrapper recognized by Claude Code"
      contains: "name: gyst:foundation-sprint"
  key_links:
    - from: "bin/install.js"
      to: "~/.claude/get-your-shit-together/"
      via: "fs.mkdirSync + copyDir"
      pattern: "mkdirSync.*get-your-shit-together"
    - from: "commands/gyst/foundation-sprint.md"
      to: "~/.claude/get-your-shit-together/workflows/foundation-sprint.md"
      via: "@-reference in <execution_context>"
      pattern: "@.*get-your-shit-together/workflows/foundation-sprint"
---

<objective>
Create the npm package machinery for GYST: package.json, the interactive installer (bin/install.js), and the slash command wrapper file. After this plan, running `node bin/install.js` will copy the package's files to the correct locations under `~/.claude/`.

Purpose: Establishes the delivery mechanism so GYST can be installed via `npx` and Claude Code can discover the `/gyst:foundation-sprint` command.

Output: package.json, bin/install.js, commands/gyst/foundation-sprint.md
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Locked decisions from CONTEXT.md — honor exactly -->

Package structure:
- Repo root (C:/Users/raphg/Desktop/GYST/) is the npm package
- Install target: ~/.claude/get-your-shit-together/
- Commands install to: ~/.claude/commands/gyst/
- Agent also copied to: ~/.claude/agents/gyst-researcher.md (flat Claude Code discovery path)
- Package dirs: bin/, commands/gyst/, get-your-shit-together/workflows/, get-your-shit-together/agents/, get-your-shit-together/templates/
- Dev workflow: `node bin/install.js`

Installer (bin/install.js):
- Claude-only (no multi-runtime branching)
- No external dependencies — only Node.js built-ins: fs, path, os, readline
- Interactive: banner → confirm install location → copy files → success message
- Must check process.stdin.isTTY; auto-install if non-interactive

Slash command wrapper (commands/gyst/foundation-sprint.md):
- Thin wrapper only — logic lives in the workflow file
- Frontmatter: name, description, allowed-tools (YAML array)
- @-reference path hardcoded: @~/.claude/get-your-shit-together/workflows/foundation-sprint.md

From RESEARCH.md — confirmed patterns:
- bin/install.js shebang: #!/usr/bin/env node (line 1, no blank lines before)
- ANSI colors: cyan='\x1b[36m', green='\x1b[32m', yellow='\x1b[33m', dim='\x1b[2m', reset='\x1b[0m'
- Directory creation: fs.mkdirSync(path, { recursive: true }) — always use recursive
- File copy: fs.copyFileSync(src, dest)
- copyDir helper: readdirSync with withFileTypes: true, recurse on isDirectory()
- TTY check: if (!process.stdin.isTTY) { install(); finish(); } else { promptConfirm(...) }
- promptConfirm: readline.createInterface, default Y on empty input

package.json bin field: { "get-your-shit-together": "bin/install.js" }
package.json files array: ["bin", "commands", "get-your-shit-together", "README.md"]

Anti-pattern warnings:
- Never use ~ in Node.js paths — use os.homedir()
- Never concatenate paths with \ — use path.join()
- Always { recursive: true } on mkdirSync
- Call mkdirSync on destDir before copyFileSync
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json</name>
  <files>package.json</files>
  <action>
Create package.json in the repo root (C:/Users/raphg/Desktop/GYST/package.json) with:

```json
{
  "name": "get-your-shit-together",
  "version": "1.0.0",
  "description": "One command, one session, one testable hypothesis — Foundation Sprint for solo entrepreneurs.",
  "bin": {
    "get-your-shit-together": "bin/install.js"
  },
  "files": [
    "bin",
    "commands",
    "get-your-shit-together",
    "README.md"
  ],
  "engines": {
    "node": ">=16.7.0"
  },
  "dependencies": {},
  "license": "MIT"
}
```

Note: The npm package name (`get-your-shit-together`) is a placeholder — final name is TBD at publish time per user decisions. Do NOT add devDependencies or scripts — keep it zero-dependency.
  </action>
  <verify>node -e "const p = require('./package.json'); console.log(p.bin['get-your-shit-together'])" should print "bin/install.js"</verify>
  <done>package.json exists in repo root with correct bin, files, engines, and empty dependencies fields</done>
</task>

<task type="auto">
  <name>Task 2: Create bin/install.js — interactive installer</name>
  <files>bin/install.js</files>
  <action>
Create the bin/ directory and write bin/install.js. This is the npx entry point — a single Node.js file with no external dependencies.

Requirements:
1. First line MUST be: #!/usr/bin/env node
2. Require only built-ins: fs, path, os, readline
3. Require package.json for version display: const pkg = require('../package.json');
4. Define ANSI color constants: cyan, green, yellow, dim, reset
5. Show a banner immediately on load — ASCII art or stylized text using ANSI colors. The banner should display "GYST" prominently and the tagline "Get Your Shit Together". Design is at Claude's discretion.
6. Define the install target paths using os.homedir() (never ~):
   - homeDir = os.homedir()
   - configDir = path.join(homeDir, '.claude')
   - gystDir = path.join(configDir, 'get-your-shit-together')
   - commandsDir = path.join(configDir, 'commands', 'gyst')
   - agentsDir = path.join(configDir, 'agents')
7. implement copyDir(src, dest) helper:
   - fs.mkdirSync(dest, { recursive: true })
   - readdirSync with { withFileTypes: true }
   - recurse on isDirectory(), copyFileSync for files
8. implement install() function:
   - Create all target dirs with mkdirSync { recursive: true }
   - copyDir get-your-shit-together/ → gystDir
   - copyDir commands/gyst/ → commandsDir
   - copyFileSync agent from get-your-shit-together/agents/gyst-researcher.md → agentsDir/gyst-researcher.md (flat Claude Code discovery)
   - Print green checkmarks for each step
   - Print final success message: "Open any directory in Claude Code and run /gyst:foundation-sprint"
9. implement promptConfirm(question, defaultYes, callback):
   - If !process.stdin.isTTY → callback(true) immediately
   - Otherwise use readline.createInterface, rl.question, close on answer
   - Empty answer → use defaultYes; 'y'/'yes' → true; anything else → false
10. Main block at bottom of file:
    - If !process.stdin.isTTY: call install() then finish()
    - Else: promptConfirm("Install to ~/.claude/get-your-shit-together/?", true, callback)
    - If confirmed: install() + finish()
    - If not confirmed: print yellow "Installation cancelled" and process.exit(0)
11. finish() function prints the success summary and any next steps

The src root for copyDir calls is: path.join(__dirname, '..') — one level up from bin/.
  </action>
  <verify>node bin/install.js --help 2>/dev/null || node -e "require('./bin/install.js')" 2>&1 | head -5 — should show banner without crashing; also run: node -c bin/install.js (syntax check passes)</verify>
  <done>bin/install.js exists with shebang on line 1, passes node -c syntax check, shows banner when run, uses only Node.js built-in modules</done>
</task>

<task type="auto">
  <name>Task 3: Create commands/gyst/foundation-sprint.md — slash command wrapper</name>
  <files>commands/gyst/foundation-sprint.md</files>
  <action>
Create the commands/gyst/ directory and write commands/gyst/foundation-sprint.md.

This is a thin wrapper — it does NOT contain sprint logic. It tells Claude Code what the command is and where to load the actual workflow.

Write the file with this exact structure:

```markdown
---
name: gyst:foundation-sprint
description: Run a Foundation Sprint — one session, one testable hypothesis
allowed-tools:
  - Read
  - Write
  - Bash
  - WebSearch
  - WebFetch
argument-hint: (no arguments needed — just run it)
---

<objective>
Run the GYST Foundation Sprint end-to-end in a single session. You are a thinking partner guiding a solo entrepreneur from a fuzzy idea to a testable hypothesis using the Foundation Sprint method.

This sprint produces four output files in the user's current directory:
- COMPETITORS.md — fixed competitor list with research profiles (written after Step 1)
- HYPOTHESIS.md — the full X/Y/Z/W/U/V hypothesis + testable form (written at sprint end)
- SPRINT.md — complete journal of every decision made at each step (written at sprint end)
- POSITIONING.md — 2x2 matrix, mini-manifesto, differentiation axes (written at sprint end)
</objective>

<execution_context>
@~/.claude/get-your-shit-together/workflows/foundation-sprint.md
</execution_context>

<process>
Execute the foundation-sprint workflow from @~/.claude/get-your-shit-together/workflows/foundation-sprint.md end-to-end. Follow all instructions in that workflow precisely.
</process>
```

Key requirements:
- The `name:` field MUST be exactly `gyst:foundation-sprint` (determines the slash command name)
- The `allowed-tools:` MUST be a YAML array (not comma-separated) — command files use this format
- The @-reference in <execution_context> MUST be exactly `@~/.claude/get-your-shit-together/workflows/foundation-sprint.md` — this matches the installer's target path
- Do NOT use `tools:` (that's the agent format) — use `allowed-tools:`
  </action>
  <verify>grep -n "name: gyst:foundation-sprint" commands/gyst/foundation-sprint.md && grep -n "@~/.claude/get-your-shit-together/workflows/foundation-sprint.md" commands/gyst/foundation-sprint.md</verify>
  <done>commands/gyst/foundation-sprint.md exists with correct frontmatter (name: gyst:foundation-sprint, allowed-tools array) and @-reference pointing to the correct workflow path</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Syntax check the installer: `node -c bin/install.js` must exit 0
2. Verify package.json is valid JSON: `node -e "require('./package.json')" must exit 0`
3. Verify bin field: `node -e "const p=require('./package.json'); process.exit(p.bin['get-your-shit-together']==='bin/install.js'?0:1)"`
4. Verify command wrapper has correct name: `grep "name: gyst:foundation-sprint" commands/gyst/foundation-sprint.md`
5. Verify @-reference path: `grep "get-your-shit-together/workflows/foundation-sprint" commands/gyst/foundation-sprint.md`

Note: Full end-to-end install test (node bin/install.js) requires Plan 02 files to exist first (the installer copies get-your-shit-together/ contents). Run the full install test only after Plan 02 completes.
</verification>

<success_criteria>
- package.json exists with correct bin, files, engines, zero dependencies
- bin/install.js exists with #!/usr/bin/env node shebang, passes syntax check, uses only built-in modules
- commands/gyst/foundation-sprint.md exists with name: gyst:foundation-sprint and correct @-reference
- No external npm packages required
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-01-SUMMARY.md` using the summary template.
</output>
