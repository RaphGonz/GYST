---
phase: 04-approaches-hypothesis-outputs-steps-3-4
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - get-your-shit-together/workflows/foundation-sprint.md
autonomous: true
requirements: [SPRINT-15, SPRINT-16, OUTPUT-01, OUTPUT-02, OUTPUT-03]

must_haves:
  truths:
    - "After the chosen approach is committed, a Step 4 banner appears showing Segment, Problem, Approach, Adversary, and Axes — all pre-loaded from session context"
    - "AI pre-fills the full hypothesis template (If we help X solve Y with Z, they will choose us over W because we are U and V) from session context; user edits any slot they disagree with"
    - "Sprint does not advance from hypothesis to output files until user says 'lock it', 'locked', 'finalize', or equivalent explicit lock language — a simple 'yes' or 'looks good' is not a lock"
    - "After hypothesis is locked, AI automatically derives all 4 testable form components: success metric, falsification condition, main risk, fastest validation test — no per-field user input required"
    - "All 3 output files (HYPOTHESIS.md, SPRINT.md, POSITIONING.md) are written together in the section_write_outputs section — no output files are written anywhere else in the workflow"
    - "HYPOTHESIS.md contains: full hypothesis statement, 6-variable breakdown table, and all 4 testable form components — no square brackets remain"
    - "SPRINT.md contains: all 4 steps fully populated — customer, problem, founder advantages, competitors (Step 1); axis ratings, axes chosen, competitor positions, manifesto (Step 2); all approach descriptions A1-A[N], 4-matrix evaluation table, recommendation, backup, chosen approach (Step 3); full hypothesis statement (Step 4) — no square brackets remain"
    - "POSITIONING.md contains: both differentiating axes with descriptions and rationale, the Step 2 ASCII 2x2 matrix with COMPETITOR names (not approach labels), competitor positions table, and mini-manifesto — no square brackets remain"
    - "Sprint ends with a closing message listing all 3 files written and naming the fastest validation test from HYPOTHESIS.md"
  artifacts:
    - path: "get-your-shit-together/workflows/foundation-sprint.md"
      provides: "Complete Step 4 flow: step4_banner, section_hypothesis, section_testable_form, section_write_outputs — sprint is complete end-to-end with no stubs"
      contains: "step4_banner, section_hypothesis, section_testable_form, section_write_outputs"
  key_links:
    - from: "section_approach_recommendation (Plan 04-01)"
      to: "step4_banner"
      via: "direct flow after user commits to chosen approach"
      pattern: "step4_banner"
    - from: "step4_banner"
      to: "section_hypothesis"
      via: "immediate flow after banner renders"
      pattern: "section_hypothesis"
    - from: "section_hypothesis"
      to: "section_testable_form"
      via: "explicit lock trigger from user ('lock it' or equivalent)"
      pattern: "section_testable_form"
    - from: "section_testable_form"
      to: "section_write_outputs"
      via: "user confirms ready after testable form is displayed"
      pattern: "section_write_outputs"
    - from: "section_write_outputs"
      to: "HYPOTHESIS.md, SPRINT.md, POSITIONING.md (runtime files)"
      via: "Write tool calls — all 3 at sprint end, reading templates first"
      pattern: "section_write_outputs.*ONLY place"
---

<objective>
Append Step 4 (Final Hypothesis) and the sprint end ceremony to foundation-sprint.md: Step 4 stage transition banner, pre-filled hypothesis template with explicit lock requirement, auto-derived testable form, and all three output file writers (HYPOTHESIS.md, SPRINT.md, POSITIONING.md) in a single sprint end section.

Purpose: This plan closes the sprint end-to-end. After the user commits to an approach in Step 3 (Plan 04-01), this plan delivers the final intellectual product: a locked hypothesis and its testable form. The output files create permanent artifacts the user keeps after the session ends. This is the payoff of the entire Foundation Sprint — one testable hypothesis, grounded in real market positioning, derived from the user's actual capabilities.

Output: foundation-sprint.md extended from approximately 1,100 lines to approximately 1,350 lines. Four new named sections appended: step4_banner, section_hypothesis, section_testable_form, section_write_outputs. No stubs remain anywhere in the file — the sprint is complete end-to-end.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-approaches-hypothesis-outputs-steps-3-4/04-CONTEXT.md
@.planning/phases/04-approaches-hypothesis-outputs-steps-3-4/04-RESEARCH.md
@.planning/phases/04-approaches-hypothesis-outputs-steps-3-4/04-01-SUMMARY.md
</context>

<interfaces>
<!-- Key interfaces the executor needs. -->

From Plan 04-01 output (sections that must already exist before this plan runs):
- step3_banner — Step 3 banner; re-rendered after approach is committed
- section_context_reload — displays Capacity, Insight, axes from session
- section_approach_generation — user-first approach + probe + AI keep/drop loop
- section_approach_evaluation — 4 sequential ASCII matrices
- section_approach_recommendation — global pattern recommendation + user commits to chosen approach

Hypothesis template variables and their sources:
| Variable | Label | Source |
|----------|-------|--------|
| X | target customer | Step 1 — locked Customer Segment |
| Y | core problem | Step 1 — locked Core Problem |
| Z | approach | Step 3 — committed approach (chosen in section_approach_recommendation) |
| W | main adversary | Step 1 — main adversary flagged in COMPETITORS.md selection |
| U | differentiator 1 | Step 2 — Phrase 1 from mini-manifesto (locked in section_manifesto) |
| V | differentiator 2 | Step 2 — Phrase 2 from mini-manifesto (locked in section_manifesto) |

Full hypothesis sentence: "If we help X solve Y with Z, they will choose us over W because we are U and V"

Output file template paths (read at write time, not at workflow load time):
- HYPOTHESIS.md: @~/.claude/get-your-shit-together/templates/HYPOTHESIS.md
- SPRINT.md: @~/.claude/get-your-shit-together/templates/SPRINT.md
- POSITIONING.md: @~/.claude/get-your-shit-together/templates/POSITIONING.md

POSITIONING.md matrix note: The 2x2 matrix in POSITIONING.md shows COMPETITORS from Step 2 plotted on the two differentiating axes. It does NOT show A1/A2/A3/A4 approach labels. The approach evaluation exists only in SPRINT.md.

Explicit lock language that triggers hypothesis finalization:
- "lock it", "locked", "finalize", "done", "that's it", "confirmed"
- A simple "yes" or "looks good" or "that works" is NOT a lock — ask "Ready to lock this?" if ambiguous
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add step4_banner and section_hypothesis</name>
  <files>get-your-shit-together/workflows/foundation-sprint.md</files>
  <action>
Read get-your-shit-together/workflows/foundation-sprint.md (already extended through section_approach_recommendation from Plan 04-01). Append the following two blocks at the end of the file:

```markdown
<step4_banner>
<!-- BANNER RENDER INSTRUCTION — Step 4 stage transition. Render immediately after approach is committed in section_approach_recommendation. -->

The Step 4 banner format:

─── Step 4: Final Hypothesis ─────────────────────
Segment:   [X — target customer from Step 1]
Problem:   [Y — core problem from Step 1]
Approach:  [Z — chosen approach from Step 3]
Adversary: [W — main adversary from Step 1]
Axes:      [U — Differentiator 1 from manifesto]
           [V — Differentiator 2 from manifesto]
Hypothesis: pending
──────────────────────────────────────────────────

Rules: Same visual style as Steps 1, 2, 3 banners. No emoji. Width ~50 chars.
Render all 6 variables filled in from session context — do NOT leave any variable as "[placeholder]".
</step4_banner>

<section name="section_hypothesis">

## Step 4: Final Hypothesis (SPRINT-15)

**When entering this section:** Immediately after the Step 4 banner renders.

Pre-fill the hypothesis from session context. Read each variable from the conversation — do NOT ask the user to repeat anything.

Say:

> "Here's your hypothesis, built from everything we've decided:
>
> **If we help** [X — target customer segment from Step 1]
> **solve** [Y — core problem from Step 1]
> **with** [Z — chosen approach from Step 3],
> **they will choose us over** [W — main adversary flagged in Step 1]
> **because we are** [U — Phrase 1 from Step 2 mini-manifesto] **and** [V — Phrase 2 from Step 2 mini-manifesto].
>
> Edit any part you'd change, or say **"lock it"** to finalize."

Wait for user response.

**Iteration loop:**
- If user edits one or more variables: update those variables, display the full hypothesis sentence again, ask "Anything else to change, or lock it?"
- If user says "lock it" / "locked" / "finalize" / "done" / "that's it" / "confirmed": lock the hypothesis. Proceed to section_testable_form.
- A simple "yes", "looks good", "that works", or "ok" is NOT a lock. Ask "Ready to lock this hypothesis?" if the response is ambiguous.

Do NOT advance to section_testable_form until explicit lock language is received.

</section>
```

After writing, verify both blocks are present and that the section_hypothesis contains the lock language rules.
  </action>
  <verify>
    <automated>grep -q "step4_banner" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "section_hypothesis" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "lock it" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "is NOT a lock" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "SPRINT-15" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    step4_banner and section_hypothesis are present. The hypothesis section contains the explicit lock trigger rules ("lock it", "locked", "finalize" accepted; "yes" / "looks good" explicitly NOT a lock). SPRINT-15 is referenced in the section header. The Step 4 banner format shows all 6 variables (X, Y, Z, W, U, V).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add section_testable_form and section_write_outputs</name>
  <files>get-your-shit-together/workflows/foundation-sprint.md</files>
  <action>
Read get-your-shit-together/workflows/foundation-sprint.md (already extended through section_hypothesis from Task 1). Append the following two sections at the end of the file:

```markdown
<section name="section_testable_form">

## Testable Form (SPRINT-16)

**When entering this section:** Immediately after the hypothesis is locked.

Derive all 4 testable form components automatically from the locked hypothesis. Do NOT ask the user for input on these — they are AI-derived from the locked content.

| Component | What it is | How to derive it |
|-----------|-----------|-----------------|
| Success metric | Observable, measurable sign the hypothesis is working | What "enough customers choosing Z to solve Y" looks like as a specific number + timeframe |
| Falsification condition | The specific threshold at which the hypothesis is proven wrong | N outreach attempts with M% conversion as a concrete failure boundary |
| Main risk | The single biggest assumption that could kill this | What about X, Y, or Z is most uncertain or unproven |
| Fastest validation test | The cheapest experiment to confirm or kill the hypothesis first | Manual validation, landing page test, or direct outreach |

Display all 4 components together:

> "**Your hypothesis in testable form:**
>
> **Success metric:** [specific, measurable — number + timeframe]
> **Falsification condition:** [specific threshold — if X then proven wrong]
> **Main risk:** [one sentence — the assumption most likely to be false]
> **Fastest validation test:** [one concrete experiment to run first]
>
> These are locked with your hypothesis. Ready to write your output files?"

Wait for user to confirm readiness before proceeding to section_write_outputs.

</section>

<section name="section_write_outputs">

## Sprint End — Output Files (OUTPUT-01, OUTPUT-02, OUTPUT-03)

**When entering this section:** After testable form is displayed and user confirms readiness.

This is the ONLY place in the entire workflow where HYPOTHESIS.md, SPRINT.md, and POSITIONING.md are written. Do NOT write these files anywhere else.

Say: "Sprint complete. Writing your 3 output files now."

**1. Write HYPOTHESIS.md**

Read template for structure:
@~/.claude/get-your-shit-together/templates/HYPOTHESIS.md

Write ./HYPOTHESIS.md with ALL of the following — no template placeholders, no square brackets in the output:
- The full hypothesis statement as a single sentence: "If we help X solve Y with Z, they will choose us over W because we are U and V"
- Breakdown table with all 6 variables explicitly labeled: X (target customer), Y (core problem), Z (chosen approach), W (main adversary), U (differentiator 1), V (differentiator 2)
- Success metric (from testable form above)
- Falsification condition (from testable form above)
- Main risk (from testable form above)
- Fastest validation test (from testable form above)

CRITICAL: Zero square brackets remain in HYPOTHESIS.md. No field may say "[placeholder]" or "[TARGET CUSTOMER: ...]".

**2. Write SPRINT.md**

Read template for structure:
@~/.claude/get-your-shit-together/templates/SPRINT.md

Write ./SPRINT.md with ALL of the following — no template placeholders, no square brackets:
- **Step 1:** target customer (options considered, chosen, rationale), core problem (options considered, chosen, validation result), founder advantages (Capacity statement, Insight statement, Motivation statement), competitors (all listed, main adversary flagged, one-line research summary per competitor)
- **Step 2:** all axis ratings (all 8+ axes with the user's score for each), chosen X-axis and Y-axis with rationale, conflict check result (whether a conflict was found and how it was resolved), mini-manifesto (all 3 phrases verbatim)
- **Step 3:** all approach descriptions (A1 through A[N] — each with short name and full 2-3 sentence description), 4-matrix evaluation table (each approach's quadrant placement in each of the 4 matrices), recommended approach (which A# and why), backup approach (which A# and why), chosen approach (which A# the user selected)
- **Step 4:** full hypothesis statement (must match HYPOTHESIS.md exactly, character for character)

CRITICAL: Zero square brackets remain in SPRINT.md. Every section has real content from the session.

**3. Write POSITIONING.md**

Read template for structure:
@~/.claude/get-your-shit-together/templates/POSITIONING.md

Write ./POSITIONING.md with ALL of the following — no template placeholders, no square brackets:
- X-axis (from Step 2): axis name, description of what it measures, rationale for choosing it as a differentiator
- Y-axis (from Step 2): axis name, description of what it measures, rationale for choosing it as a differentiator
- The Step 2 2x2 ASCII matrix — the SAME matrix from section_matrix_render showing COMPETITORS positioned on the two differentiating axes. This matrix shows COMPETITOR NAMES (from COMPETITORS.md), NOT approach labels (A1/A2/A3). The approach evaluation matrices from Step 3 do NOT appear in POSITIONING.md.
- Competitor positions table: each competitor from Step 1 with their X-axis score, Y-axis score, quadrant, and 1-sentence rationale (sourced from the scoring in section_competitor_scoring)
- Mini-manifesto: all 3 phrases verbatim from Step 2 (Differentiator 1, Differentiator 2, Safeguard)

CRITICAL: POSITIONING.md's matrix uses competitor names — it does NOT use A1/A2/A3/A4. The approach evaluation exists only in SPRINT.md.
CRITICAL: Zero square brackets remain in POSITIONING.md.

**After all 3 files are written:**

> "Done. Your Foundation Sprint is complete.
>
> **Files written to your project directory:**
> - `HYPOTHESIS.md` — your testable hypothesis
> - `SPRINT.md` — the complete decision journal
> - `POSITIONING.md` — your positioning map and manifesto
>
> **Your next move:** [fastest validation test from the testable form]"

</section>
```

After writing, run `node bin/install.js` to push changes to ~/.claude/get-your-shit-together/. Run from the GYST repo root (C:/Users/raphg/Desktop/GYST), not from inside the package directory.
  </action>
  <verify>
    <automated>grep -q "section_testable_form" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "section_write_outputs" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "ONLY place in the entire workflow" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "POSITIONING.md.*matrix uses competitor names" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "HYPOTHESIS.md" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "SPRINT.md" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "Zero square brackets remain" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && wc -l "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" | awk '{if ($1 > 1200) print "PASS"; else print "FAIL: file too short, got " $1}'</automated>
  </verify>
  <done>
    section_testable_form and section_write_outputs are both present. "ONLY place in the entire workflow" appears to prevent scattered writes. The POSITIONING.md matrix clarification (competitor names, not approach labels) is explicit. "Zero square brackets remain" appears for each output file. File is longer than 1,200 lines. install.js has been run and changes are live in ~/.claude/get-your-shit-together/workflows/foundation-sprint.md.
  </done>
</task>

</tasks>

<verification>
After both tasks, the complete Step 4 and sprint end ceremony are in place. Verify the full sequence:

1. `grep -c "step4_banner\|section_hypothesis\|section_testable_form\|section_write_outputs" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md"` — must return 4
2. `grep -q "is NOT a lock" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md"` — must pass (explicit lock required)
3. `grep -q "ONLY place in the entire workflow" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md"` — must pass (single write location)
4. `grep -q "Zero square brackets remain" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md"` — must pass (no-placeholder rule present)
5. `grep -q "approach labels\|A1/A2/A3" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md"` — must pass (POSITIONING.md matrix disambiguation present)
6. `wc -l "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" | awk '{if ($1 > 1200) print "PASS"; else print "FAIL"}'` — must pass
7. No stubs remain: `grep -q "not yet implemented\|Stay tuned for the next release\|stub" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md"` — must FAIL
8. Install ran: `ls -la "C:/Users/raphg/.claude/get-your-shit-together/workflows/foundation-sprint.md"` shows recent modification timestamp
</verification>

<success_criteria>
- After approach is committed, Step 4 banner appears with all 6 hypothesis variables pre-loaded from session context
- AI pre-fills the full hypothesis template; user can edit any slot; sprint does not auto-advance on vague confirmation
- Sprint requires explicit lock language before writing output files
- After hypothesis is locked, AI auto-derives all 4 testable form components (no per-field user input)
- All 3 output files are written together in section_write_outputs — no output file writes anywhere else in the workflow
- HYPOTHESIS.md contains the full hypothesis sentence + 6-variable breakdown + 4 testable form components, with zero template placeholders
- SPRINT.md captures all 4 steps in full (all approaches, all matrix placements, chosen approach, final hypothesis), with zero template placeholders
- POSITIONING.md shows the Step 2 competitor matrix (not Step 3 approach labels) plus axes, competitor positions table, and manifesto, with zero template placeholders
- Sprint ends with closing message naming all 3 files and the fastest validation test
- No stubs remain in foundation-sprint.md — the sprint is complete end-to-end
- All changes are live in ~/.claude/get-your-shit-together/ after install.js runs
</success_criteria>

<output>
After completion, create `.planning/phases/04-approaches-hypothesis-outputs-steps-3-4/04-02-SUMMARY.md` following the summary template at @C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</output>
