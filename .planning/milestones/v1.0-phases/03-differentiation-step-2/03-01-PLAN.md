---
phase: 03-differentiation-step-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-your-shit-together/workflows/foundation-sprint.md
autonomous: true
requirements: [SPRINT-07, SPRINT-08]

must_haves:
  truths:
    - "When Step 2 opens, a banner displays with X-axis, Y-axis, dream company position, and manifesto all as 'pending'"
    - "User sees all 8 standard bipolar axes at once, each with pole labels on both ends and a -5 to +5 scale"
    - "User can rate all 8 axes in a single comma-separated reply or one at a time"
    - "After rating confirmation, an optional custom axes step appears where both AI and user can propose domain-specific axes"
    - "User picks exactly 2 differentiating axes freely from all rated axes; AI does not suggest which 2 to pick"
    - "After axis selection, the Step 2 banner re-renders with the locked X-axis, Y-axis, and dream company scores"
  artifacts:
    - path: "get-your-shit-together/workflows/foundation-sprint.md"
      provides: "Step 2 axis rating and selection sections replacing the step2_stub"
      contains: "step2_banner, section_axis_rating, section_custom_axes, section_axis_selection"
  key_links:
    - from: "step2_stub (lines 509-518)"
      to: "step2_banner + section_axis_rating + section_custom_axes + section_axis_selection"
      via: "direct replacement of the stub block"
      pattern: "step2_banner|section_axis_rating|section_custom_axes|section_axis_selection"
    - from: "section_axis_rating"
      to: "section_custom_axes"
      via: "sequential flow after ratings locked"
      pattern: "section_custom_axes"
    - from: "section_custom_axes"
      to: "section_axis_selection"
      via: "optional step, always exits to axis selection"
      pattern: "section_axis_selection"
---

<objective>
Replace the `<step2_stub>` block in foundation-sprint.md with the first half of Step 2: the opening banner, the 8-axis rating section, the custom axis proposal step, and the differentiating axis selection with banner lock.

Purpose: Give users the quantitative foundation they need before any competitor scoring can happen. All 8 ratings (plus any custom axes) must be locked before the user picks their 2 differentiating axes — the sequence is non-negotiable.

Output: foundation-sprint.md extended from 518 lines to ~620 lines. The step2_stub is entirely replaced. Four new named sections are live: step2_banner, section_axis_rating, section_custom_axes, section_axis_selection.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-differentiation-step-2/03-CONTEXT.md
@.planning/phases/03-differentiation-step-2/03-RESEARCH.md
@.planning/phases/02-the-basics-step-1/02-02-SUMMARY.md
</context>

<interfaces>
<!-- Key interfaces the executor needs. Extracted from codebase. -->

From get-your-shit-together/workflows/foundation-sprint.md (lines 509-518, the block to REPLACE):
```markdown
<step2_stub>
<!-- STEP 2 STUB: Phase 3 fills in Differentiation logic -->

After Step 1 navigation confirms "advance to Step 2", tell the user:

> "Step 2 (Differentiation) is not yet implemented in this version. Your COMPETITORS.md has been saved to your project directory. Stay tuned for the next release."

Do not attempt to run Step 2 logic. The sprint ends here for now.

</step2_stub>
```

From foundation-sprint.md (step1_banner pattern, already in file — follow this exact visual style):
```
─── Step 1: The Basics ──────────────────────────
Customer: [value or "pending"]
Problem: [value or "pending"]
Advantages: [locked / pending]
Competitors: [value or "pending"]
─────────────────────────────────────────────────
```

From COMPETITORS.md template (fields available for scoring in Plan 03-02):
- `What they do`, `Pricing model`, `Known strengths`, `Known weaknesses`, `Positioning signals`
- Main adversary flagged with `* MAIN ADVERSARY`

Standard 8 bipolar axes (per RESEARCH.md — this is authoritative, use exactly these):
1. Slow ←→ Fast
2. Hard ←→ Easy
3. Expensive ←→ Free
4. Complex ←→ Simple
5. Dumb ←→ Smart
6. Siloed ←→ Integrated
7. Manual ←→ Automatic
8. Narrow ←→ Broad   ← 8th axis is Claude's Discretion; "Narrow ←→ Broad" chosen
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace step2_stub with step2_banner and section_axis_rating</name>
  <files>get-your-shit-together/workflows/foundation-sprint.md</files>
  <action>
Read get-your-shit-together/workflows/foundation-sprint.md in full. Locate the `<step2_stub>` block (lines 509-518, at the very end of the file). Replace the entire `<step2_stub>...</step2_stub>` block with the following new content (keep everything above it untouched):

```markdown
<step2_banner>
<!-- BANNER RENDER INSTRUCTION — reusable for Step 2. Render on Step 2 entry AND after axes are locked. -->

The Step 2 banner format:

─── Step 2: Differentiation ─────────────────────
X-axis: [value or "pending"]
Y-axis: [value or "pending"]
Dream company: [X score, Y score or "pending"]
Manifesto: [locked / pending]
─────────────────────────────────────────────────

After axes are locked (example with real values):
─── Step 2: Differentiation ─────────────────────
X-axis: Manual ←→ Automatic (You: +4)
Y-axis: Expensive ←→ Free (You: +3)
Dream company: top-right (+4, +3)
Manifesto: pending
─────────────────────────────────────────────────

Rules: Same visual style as Step 1 banner. No emoji. Width ~50 chars. Show locked values inline; "pending" for not-yet-decided.
</step2_banner>

<section name="section_axis_rating">

## Step 2: Rating the Dream Company

**When entering this section:** Render the Step 2 banner with all four values as "pending".

Then say:

> "Now we position your dream company on 8 bipolar axes. Each axis has two poles — rate where YOUR DREAM COMPANY sits.
>
> Scale: -5 = far left pole, 0 = neutral, +5 = far right pole
>
> 1. Slow ←————————→ Fast
> 2. Hard ←————————→ Easy
> 3. Expensive ←———→ Free
> 4. Complex ←—————→ Simple
> 5. Dumb ←————————→ Smart
> 6. Siloed ←——————→ Integrated
> 7. Manual ←——————→ Automatic
> 8. Narrow ←——————→ Broad
>
> Reply with 8 numbers in order, e.g.: "+3, -1, +5, +2, +4, 0, +3, +2"
> Or rate them one at a time — your choice."

Wait for the user to respond.

Accept any readable format: comma-separated list, numbered list, or axis-by-axis. Parse the 8 values.

Confirm back with all 8 ratings listed:

> "Got it. Your dream company ratings:
>
> 1. Slow ←→ Fast: [score]
> 2. Hard ←→ Easy: [score]
> 3. Expensive ←→ Free: [score]
> 4. Complex ←→ Simple: [score]
> 5. Dumb ←→ Smart: [score]
> 6. Siloed ←→ Integrated: [score]
> 7. Manual ←→ Automatic: [score]
> 8. Narrow ←→ Broad: [score]
>
> Are these right? (Yes to lock, or tell me what to change.)"

Wait for confirmation. Accept on first confirmation — do not push back on ratings.

After confirmation: lock all 8 ratings. Proceed to section_custom_axes.

</section>
```

After writing, verify the file no longer contains `<step2_stub>` and now contains both `step2_banner` and `section_axis_rating`.
  </action>
  <verify>
    <automated>grep -c "step2_stub" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" | grep -q "^0$" && grep -q "step2_banner" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "section_axis_rating" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    step2_stub is gone. step2_banner and section_axis_rating are present. Foundation-sprint.md is longer than 518 lines. The file reads as valid markdown — no truncation or corruption.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add section_custom_axes and section_axis_selection</name>
  <files>get-your-shit-together/workflows/foundation-sprint.md</files>
  <action>
Read get-your-shit-together/workflows/foundation-sprint.md (already extended from Task 1). Append the following two sections at the end of the file (after `</section>` closing tag of section_axis_rating):

```markdown
<section name="section_custom_axes">

## Custom Axes (optional but prominent)

**When entering this section:** After all 8 standard axis ratings are locked.

Analyze the competitors' industries and profiles from the Step 1 conversation context (the competitor names and any market signals already discussed). Propose 1-2 domain-specific axes that would be meaningful differentiators in this particular market.

Say:

> "Now let's think about axes specific to your market.
>
> Based on your competitors' profiles, I'd suggest these domain-specific axes:
>
> **A) [AI-proposed axis 1 name]:** [Left pole] ←→ [Right pole]
>    *Why: [one sentence — what this measures and why it matters in your specific market]*
>
> **B) [AI-proposed axis 2 name]:** [Left pole] ←→ [Right pole]
>    *Why: [one sentence]*
>
> Do you want to add any of these, or propose your own?
>
> - Type 'A', 'B', or 'A and B' to add my suggestions (and I'll ask you to rate them)
> - Describe your own axis (give it a name and two poles)
> - Type 'skip' to work with just the standard 8"

Wait for user response.

If they accept one or both AI axes: ask them to rate each accepted axis on -5 to +5 immediately. Lock the custom axis scores alongside the standard 8.

If they propose their own axis: accept the name and poles, ask them to rate it on -5 to +5, lock it.

If they skip: acknowledge and proceed immediately to section_axis_selection.

**Do NOT suggest which 2 axes to use as differentiators.** The custom axis step only adds axes to the rated pool — selection happens in the next section.

After all accepted custom axes are rated and locked: proceed to section_axis_selection.

</section>

<section name="section_axis_selection">

## Selecting 2 Differentiating Axes

**When entering this section:** After all axes (standard 8 + any custom) are rated and locked.

List all rated axes with their scores so the user can see them at a glance:

> "Here are all your rated axes. Pick the 2 that best capture where YOUR dream company is different from the competitors:
>
> 1. Slow ←→ Fast: [score]
> 2. Hard ←→ Easy: [score]
> 3. Expensive ←→ Free: [score]
> 4. Complex ←→ Simple: [score]
> 5. Dumb ←→ Smart: [score]
> 6. Siloed ←→ Integrated: [score]
> 7. Manual ←→ Automatic: [score]
> 8. Narrow ←→ Broad: [score]
> [9+. Custom axes if any, with scores]
>
> Which 2 do you want as your X-axis and Y-axis?
> (e.g., '3 and 7' or 'Expensive-Free as X, Manual-Automatic as Y')"

Wait for user response.

**Do NOT suggest or recommend any axes.** Accept whatever 2 the user picks without commentary on whether they're "good" choices.

After user picks their 2 axes: confirm back:

> "Got it:
> X-axis: [axis name] — You: [score]
> Y-axis: [axis name] — You: [score]
>
> Locking these as your differentiating axes."

Re-render the Step 2 banner with the locked axis names and the dream company's scores on each:

─── Step 2: Differentiation ─────────────────────
X-axis: [axis] (You: [X score])
Y-axis: [axis] (You: [Y score])
Dream company: top-right ([X score], [Y score])
Manifesto: pending
─────────────────────────────────────────────────

After the banner: proceed to section_competitor_scoring (implemented in Plan 03-02).

</section>
```

After writing, confirm section_custom_axes and section_axis_selection are both present in the file.
  </action>
  <verify>
    <automated>grep -q "section_custom_axes" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "section_axis_selection" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && wc -l "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" | awk '{if ($1 > 580) print "PASS"; else print "FAIL: too short, got " $1}'</automated>
  </verify>
  <done>
    section_custom_axes and section_axis_selection are present. File is longer than 580 lines. The axis selection section ends with a reference to section_competitor_scoring. The file has no stub text remaining. No web search calls appear anywhere in these sections.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -c "step2_stub" get-your-shit-together/workflows/foundation-sprint.md` returns 0
2. All four section names present: step2_banner, section_axis_rating, section_custom_axes, section_axis_selection
3. File line count greater than 580
4. No WebSearch or WebFetch instructions appear in the new sections
5. AI does not suggest which 2 axes to pick as differentiators (search for "recommend" or "suggest" near axis_selection — should not appear)
6. section_axis_rating instructs to accept comma-separated reply and confirm before locking
7. section_custom_axes has both AI proposal and skip option
8. section_axis_selection ends with re-render of step2_banner showing locked values
</verification>

<success_criteria>
- The step2_stub block is completely replaced
- A user entering Step 2 sees the banner immediately on entry
- A user can rate all 8 axes in one message and confirm
- A user can optionally add custom axes (AI proposes 2, user can skip)
- A user picks their own 2 differentiating axes with no AI suggestion
- The Step 2 banner re-renders with locked axis names and dream company scores
- No web searching occurs anywhere in these sections
- The file is valid markdown, no truncation
</success_criteria>

<output>
After completion, create `.planning/phases/03-differentiation-step-2/03-01-SUMMARY.md` following the summary template at @C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</output>
