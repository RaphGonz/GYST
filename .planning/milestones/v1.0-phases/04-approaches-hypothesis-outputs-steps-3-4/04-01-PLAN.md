---
phase: 04-approaches-hypothesis-outputs-steps-3-4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-your-shit-together/workflows/foundation-sprint.md
autonomous: true
requirements: [SPRINT-12, SPRINT-13, SPRINT-14]

must_haves:
  truths:
    - "Step 3 opens by displaying the locked Capacity statement, locked Insight statement, and both locked differentiating axes from the session — no re-asking the user"
    - "AI waits for user to propose their own approach before generating any alternatives — no AI approaches appear until A1 is finalized"
    - "AI asks 1-2 probe questions in a single message after the user proposes their approach, then records it as A1"
    - "AI-generated approaches (A2, A3, A4) are proposed one at a time with keep/drop prompts — filtered silently by Capacity, Insight, and axis fit; no mention of filtered candidates"
    - "After 3-4 approaches are finalized, each is evaluated on 4 ASCII 2x2 matrices shown sequentially (one at a time, not all at once)"
    - "Matrix order is: Customer Vision → Money Vision → Pragmatic Vision → Growth Vision"
    - "After all 4 matrices, AI names the approach with the strongest global pattern as winner and names the second-best as backup"
    - "User can override AI's recommendation freely; AI acknowledges and proceeds without pushback"
    - "The step3_stub block (lines 887-903) is completely replaced — no stub text remains"
  artifacts:
    - path: "get-your-shit-together/workflows/foundation-sprint.md"
      provides: "Step 3 full flow: banner, context reload, approach generation, 4-matrix evaluation, recommendation, and flow entry point to step4_banner"
      contains: "step3_banner, section_context_reload, section_approach_generation, section_approach_evaluation, section_approach_recommendation"
  key_links:
    - from: "section_step2_navigation (Plan 03-02)"
      to: "step3_banner"
      via: "direct flow after user chooses A (advance to Step 3)"
      pattern: "step3_banner"
    - from: "section_context_reload"
      to: "section_approach_generation"
      via: "after user responds with their initial approach idea"
      pattern: "section_approach_generation"
    - from: "section_approach_generation"
      to: "section_approach_evaluation"
      via: "after 3-4 approaches are finalized and user confirms ready"
      pattern: "section_approach_evaluation"
    - from: "section_approach_evaluation"
      to: "section_approach_recommendation"
      via: "immediately after Matrix 4 (Growth Vision) is shown"
      pattern: "section_approach_recommendation"
    - from: "section_approach_recommendation"
      to: "step4_banner"
      via: "after user commits to chosen approach"
      pattern: "step4_banner"
---

<objective>
Replace the step3_stub (lines 887-903 of foundation-sprint.md) with the complete Step 3 flow: Step 3 banner, context reload display, user-first approach generation with AI additions and keep/drop loop, 4-matrix sequential ASCII evaluation, and global pattern recommendation with approach commitment.

Purpose: This plan completes Step 3 (Approaches) — the step where the user commits to a specific product direction that will feed the final hypothesis in Step 4. The approach must be grounded in what the founder can actually build (Capacity), what they know (Insight), and must reinforce the Step 2 differentiating axes. The 4-matrix evaluation gives the user a visual framework to compare approaches across customer, financial, operational, and growth dimensions before committing.

Output: foundation-sprint.md extended from 903 lines to approximately 1,100 lines. The step3_stub is fully replaced. Five new named sections are live: step3_banner, section_context_reload, section_approach_generation, section_approach_evaluation, section_approach_recommendation. The file flows from Step 2 navigation through to a stub entry point for step4_banner (which Plan 04-02 will fill).
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-approaches-hypothesis-outputs-steps-3-4/04-CONTEXT.md
@.planning/phases/04-approaches-hypothesis-outputs-steps-3-4/04-RESEARCH.md
</context>

<interfaces>
<!-- Key interfaces the executor needs. -->

From Plan 03-02 output (sections that must already exist in foundation-sprint.md):
- step2_banner — Step 2 banner format
- section_axis_rating — 8 standard axes rated -5 to +5
- section_custom_axes — optional domain-specific axes
- section_axis_selection — user picks 2 differentiating axes (X-axis and Y-axis), locks them
- section_competitor_scoring — reads COMPETITORS.md, derives scores per axis
- section_matrix_render — ASCII 2x2 competitor matrix + conflict detection
- section_manifesto — 3-phrase mini-manifesto; holistic evaluation
- section_step2_navigation — summary block + A (advance to Step 3) / B (go back) choice

Current file state:
- foundation-sprint.md is 903 lines
- Lines 887-903 are the step3_stub block (entire block from `<step3_stub>` to `</step3_stub>`)
- Everything above line 886 is untouched by this plan

Step 3 banner format (mirrors Steps 1 and 2):
```
─── Step 3: Approaches ──────────────────────────
Context: loading...
Approaches: pending
Chosen: pending
─────────────────────────────────────────────────
```

After approach is committed:
```
─── Step 3: Approaches ──────────────────────────
Approaches: [N] finalized (A1, A2, A3[, A4])
Recommended: [A#] — [short name]
Chosen: [A#] — [short name]
─────────────────────────────────────────────────
```

4-Matrix axes (all locked; do not alter):
| Matrix | X-axis | Y-axis |
|--------|--------|--------|
| Customer Vision | Ease to use (Hard → Easy) | How perfectly it solves the problem (Partially → Perfectly) |
| Money Vision | Revenue type (One-time → Recurring long term) | Number of customers (Few → Many) |
| Pragmatic Vision | Ease to build (Hard → Easy) | Speed to build (Slow → Fast) |
| Growth Vision | Adaptability (Rigid → Highly Adaptable) | Number of customers over time (Few → Many) |

ASCII matrix format (same as Phase 3 competitor matrix):
```
      [Y-axis high label]
              ^
  [A2]        |        [A1]
              |
──────────────+──────────────► [X-axis high label]
              |
  [A3]        |        [A4]
              |
      [Y-axis low label]
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace step3_stub with Step 3 banner, context reload, and approach generation</name>
  <files>get-your-shit-together/workflows/foundation-sprint.md</files>
  <action>
Read get-your-shit-together/workflows/foundation-sprint.md. Locate and replace the entire step3_stub block — from `<step3_stub>` through `</step3_stub>` (lines 887-903) — with the following three sections. Everything above line 886 is untouched.

Replace the entire step3_stub block with:

```markdown
<step3_banner>
<!-- BANNER RENDER INSTRUCTION — reusable for Step 3. Render on Step 3 entry AND after approach is committed. -->

The Step 3 banner format on entry:

─── Step 3: Approaches ──────────────────────────
Context: loading...
Approaches: pending
Chosen: pending
─────────────────────────────────────────────────

After approaches are finalized and the chosen approach is committed:

─── Step 3: Approaches ──────────────────────────
Approaches: [N] finalized (A1, A2, A3[, A4])
Recommended: [A#] — [short name]
Chosen: [A#] — [short name]
─────────────────────────────────────────────────

Rules: Same visual style as Steps 1 and 2 banners. No emoji. Width ~50 chars.
</step3_banner>

<section name="section_context_reload">

## Step 3: Context Reload and Approach Prompt (SPRINT-12)

**When entering this section:** Immediately after the Step 3 banner renders on entry.

Read the locked Capacity and Insight statements from earlier in this session.
Do NOT re-ask the user for this information. Do NOT skip this step.
If you cannot find the exact wording in context, display your best recollection and add "(confirm?)" — do not ask the user to repeat the full conversation.

Say:

> "Before we look at approaches, let me bring up what we established about you:
>
> **Your Capacity:** [locked Capacity statement from Step 1]
> **Your Insight:** [locked Insight statement from Step 1]
>
> **Your differentiating position:**
> - [X-axis locked from Step 2 — axis name and your score]
> - [Y-axis locked from Step 2 — axis name and your score]
>
> Any approach we consider will need to fit what you can actually build,
> draw on what you know first-hand, and reinforce where you want to sit
> relative to competitors.
>
> With that in mind — what's your initial approach idea?"

Wait for user response. Do NOT generate any approach options before the user responds.

</section>

<section name="section_approach_generation">

## Approach Generation (SPRINT-12)

**When entering this section:** After user has responded with their initial approach idea.

**Phase 1: Sharpen the user's approach (A1)**

Ask 1-2 probe questions to clarify the approach before recording it as A1.
Ask both questions together in one message — do not string probes across multiple turns.

Example probe question types (adjust to what the user actually said):
- A question about delivery mechanism: self-serve product vs. hands-on service vs. community
- A question about who experiences the core value: the end customer directly, or someone else first

Wait for user to answer. Then record the approach as A1 with a short name (2-3 words) and a 2-3 sentence description.

Say: "Got it — this is **Approach 1 (A1): [short name].** [2-3 sentence description grounded in their Capacity and Insight]"

Do NOT generate any AI-proposed approaches until A1 is finalized.

**Phase 2: AI-generated approaches (one at a time)**

INTERNAL FILTER (do NOT expose this logic to the user, do NOT mention it):
Before proposing any AI-generated approach, internally verify all three conditions:
1. Does this approach require capabilities the founder explicitly stated in their Capacity? If not — skip it silently.
2. Does this approach leverage the specific Insight the founder stated? If not — skip it silently.
3. Does this approach reinforce the differentiating axes (X-axis and Y-axis locked in Step 2)? If not — skip it silently.
Never mention what was filtered. Never say "I considered X but ruled it out." Simply propose only what passes all three checks.

For each AI-generated approach, say:

> "**Approach [N] (A[N]): [short name]**
>
> [2-3 sentence description — grounded in the founder's Capacity and Insight, constrained by differentiating axes]
>
> Keep it or drop it?"

Wait for user reaction.
- If "keep": record as A[N], assign the next number label, continue to next approach if total < 4.
- If "drop": propose a different approach (still internally filtered). Do not explain what was dropped or why.

Continue until 3-4 total approaches are finalized (A1 + 2-3 AI-generated kept approaches).

After 3-4 approaches are finalized, display the lineup:

> "Here are your [N] approaches:
> - **A1: [short name]** — [one-line summary]
> - **A2: [short name]** — [one-line summary]
> - **A3: [short name]** — [one-line summary]
> [- **A4: [short name]** — [one-line summary] (if applicable)]
>
> Ready to evaluate these across 4 lenses?"

Wait for user confirmation before proceeding to section_approach_evaluation.

</section>
```

After writing, verify the step3_stub is gone and all three new blocks are present.
  </action>
  <verify>
    <automated>grep -q "step3_banner" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "section_context_reload" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "section_approach_generation" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "INTERNAL FILTER" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && ! grep -q "Step 3 (Approaches) is not yet implemented" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    step3_banner, section_context_reload, and section_approach_generation are present. The stub message "Step 3 (Approaches) is not yet implemented in this version" is gone. The INTERNAL FILTER instruction is in section_approach_generation. The file no longer contains the step3_stub placeholder text.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add section_approach_evaluation and section_approach_recommendation</name>
  <files>get-your-shit-together/workflows/foundation-sprint.md</files>
  <action>
Read get-your-shit-together/workflows/foundation-sprint.md (already extended through section_approach_generation from Task 1). Append the following two sections at the end of the file:

```markdown
<section name="section_approach_evaluation">

## 4-Matrix Evaluation (SPRINT-13)

**When entering this section:** After all 3-4 approaches are finalized and user confirms ready to evaluate.

Walk through each of the 4 matrices sequentially — one at a time. Do NOT render all 4 matrices in a single response. Show Matrix 1, wait for the user to engage or say "next", then show Matrix 2, and so on.

**For each matrix:**
1. Name the matrix and define its two axes
2. Explain each approach's quadrant placement in 1 sentence each
3. Render the ASCII 2x2 grid with approach labels (A1, A2, A3[, A4])
4. Wait for user to say "next" or ask questions before proceeding to the next matrix

ASCII grid format (same as the Step 2 competitor matrix):

```
      [Y-axis high label]
              ^
  [top-left]  |  [top-right]
              |
──────────────+──────────────► [X-axis high label]
              |
  [bot-left]  |  [bot-right]
              |
      [Y-axis low label]
```

Quadrant placement rules:
- X-axis: positive → right half; zero or negative → left half
- Y-axis: positive → top half; zero or negative → bottom half
- Stack labels vertically if multiple approaches share a quadrant
- If a quadrant is empty, render "—" in that area

---

**Matrix 1: Customer Vision**
Axes: Ease to use (Hard → Easy) × How perfectly it solves the problem (Partially → Perfectly)

For each approach: does it require expertise or hand-holding (left) or is it self-explanatory (right)? Does it solve the problem partially (bottom) or completely as described by the user (top)?

> [Explain each approach's quadrant placement, 1 sentence each]
>
> [ASCII grid with A1/A2/A3/A4 placed in their quadrants]
>
> Ready for Matrix 2: Money Vision?

---

**Matrix 2: Money Vision**
Axes: Revenue type (One-time → Recurring long term) × Number of customers (Few → Many)

For each approach: does it generate one-time revenue (left) or recurring long-term revenue (right)? Does it naturally serve a small number of customers (bottom) or can it scale to many (top)?

> [Explain each approach's quadrant placement, 1 sentence each]
>
> [ASCII grid]
>
> Ready for Matrix 3: Pragmatic Vision?

---

**Matrix 3: Pragmatic Vision**
Axes: Ease to build (Hard → Easy) × Speed to build (Slow → Fast)

For each approach: how technically complex is it to build given the founder's stated Capacity (hard = left, easy = right)? How long until a first working version given realistic effort (slow = bottom, fast = top)?

> [Explain each approach's quadrant placement, 1 sentence each]
>
> [ASCII grid]
>
> Ready for Matrix 4: Growth Vision?

---

**Matrix 4: Growth Vision**
Axes: Adaptability (Rigid → Highly Adaptable) × Number of customers over time (Few → Many)

For each approach: how rigid is this product — does it lock into one configuration (left) or can it flex as the market shifts (right)? Will the customer base stay small niche (bottom) or can it grow to many over time (top)?

> [Explain each approach's quadrant placement, 1 sentence each]
>
> [ASCII grid]

After Matrix 4 is displayed: proceed immediately to section_approach_recommendation.

</section>

<section name="section_approach_recommendation">

## Global Pattern Recommendation (SPRINT-14)

**When entering this section:** Immediately after Matrix 4 (Growth Vision) is displayed.

Review all 4 matrices. Identify which approach has the strongest global pattern: most consistently in the top-right quadrant, fewest bottom-left placements. Name the second-best as well.

Say:

> "**Looking across all 4 matrices:**
>
> **[A#] ([short name])** has the strongest overall pattern — top-right in [Matrix X] and [Matrix Y], favorable in [Matrix Z].
>
> **My recommendation: [A#].**
>
> Second-best: **[A#] ([short name])** — strong in [dimension], weaker in [dimension].
>
> You're free to choose any approach. What's your call?"

Wait for user to name their chosen approach.

Accept the user's choice unconditionally. If they pick the recommended approach, acknowledge briefly. If they pick a different approach, acknowledge and move forward — no "are you sure?" or pushback of any kind.

After user commits, re-render the Step 3 banner with the chosen approach locked:

─── Step 3: Approaches ──────────────────────────
Approaches: [N] finalized (A1, A2, A3[, A4])
Recommended: [A#] — [short name]
Chosen: [A#] — [short name]
─────────────────────────────────────────────────

Then proceed to step4_banner.

</section>
```

After writing, run `node bin/install.js` to push changes to ~/.claude/get-your-shit-together/. Run from the GYST repo root (C:/Users/raphg/Desktop/GYST), not from inside the package directory.
  </action>
  <verify>
    <automated>grep -q "section_approach_evaluation" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "section_approach_recommendation" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "Customer Vision" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "Money Vision" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "Pragmatic Vision" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "Growth Vision" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && grep -q "unconditionally" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" && wc -l "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" | awk '{if ($1 > 1000) print "PASS"; else print "FAIL: file too short, got " $1}'</automated>
  </verify>
  <done>
    section_approach_evaluation and section_approach_recommendation are both present. All 4 matrix names (Customer Vision, Money Vision, Pragmatic Vision, Growth Vision) appear in the file. "unconditionally" appears in the recommendation section (covering the no-pushback override rule). The file is longer than 1,000 lines. install.js has been run and changes are live in ~/.claude/get-your-shit-together/workflows/foundation-sprint.md.
  </done>
</task>

</tasks>

<verification>
After both tasks, the complete Step 3 flow is in place. Verify the full sequence:

1. `grep -q "step3_stub" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md"` — must FAIL (stub is gone)
2. `grep -c "step3_banner\|section_context_reload\|section_approach_generation\|section_approach_evaluation\|section_approach_recommendation" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md"` — must return 5
3. `grep -q "INTERNAL FILTER" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md"` — must pass (silent filter present)
4. `grep -q "Do NOT generate any approach options before the user responds" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md"` — must pass (user-first constraint present)
5. `grep -q "Do NOT render all 4 matrices in a single response" "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md"` — must pass (sequential evaluation enforced)
6. `wc -l "C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md" | awk '{if ($1 > 1000) print "PASS"; else print "FAIL"}'` — must pass
7. Install ran: `ls -la "C:/Users/raphg/.claude/get-your-shit-together/workflows/foundation-sprint.md"` shows recent modification timestamp
</verification>

<success_criteria>
- Step 3 opens by displaying Capacity, Insight, and differentiating axes from session context (not re-asking)
- User proposes Approach 1 first; AI sharpens it with probe questions before recording
- AI generates additional approaches one at a time with keep/drop; silently filtered by Capacity, Insight, and axis fit
- 3-4 finalized approaches are evaluated sequentially across all 4 matrices (one matrix at a time, with ASCII grids)
- Matrix order is Customer Vision → Money Vision → Pragmatic Vision → Growth Vision
- After Matrix 4, AI recommends the strongest global pattern approach and names a backup
- User can override freely; AI accepts unconditionally
- The old step3_stub placeholder text is completely gone from the file
- All changes are live in ~/.claude/get-your-shit-together/ after install.js runs
</success_criteria>

<output>
After completion, create `.planning/phases/04-approaches-hypothesis-outputs-steps-3-4/04-01-SUMMARY.md` following the summary template at @C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</output>
