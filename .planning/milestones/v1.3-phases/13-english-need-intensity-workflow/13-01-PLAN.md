---
phase: 13-english-need-intensity-workflow
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - get-your-shit-together/workflows/foundation-sprint.md
autonomous: true
requirements: [NEED-01, NEED-02, NEED-03, NEED-04, NEED-05, NEED-06]

must_haves:
  truths:
    - "Immediately after section_problem locks, the AI presents all 6 Need Intensity dimensions with 0-10 anchors and asks user to rate them in a single response"
    - "AI performs a web search for who is trying to solve the problem and whether any solution is dominant, then calibrates user scores downward (never upward) with explicit reasoning per dimension"
    - "Formula Neglected × (Critical + Consciousness) × (Urgent + Imposed + Real) is computed and displayed with the matching tier label from the 0-6000 scale"
    - "When score is below 1000, AI suggests one tighter client segment AND one problem reframe; user can re-rate or proceed — maximum 5 loops, flow is never blocking"
    - "Competitor names found during Need Intensity web search are stored in need_intensity_competitors and reused by section_competitors — no second competitor search runs"
    - "navigation_controls DISCARD RULE includes section_need_intensity in the rollback chain"
  artifacts:
    - path: "get-your-shit-together/workflows/foundation-sprint.md"
      provides: "section_need_intensity inserted after section_problem, before section_problem_importance"
      contains: "section name=\"section_need_intensity\""
  key_links:
    - from: "section_problem (line ~252)"
      to: "section_need_intensity"
      via: "Then proceed to section_need_intensity after problem is locked"
      pattern: "proceed to section_need_intensity"
    - from: "section_need_intensity"
      to: "section_problem_importance"
      via: "Then proceed to section_problem_importance"
      pattern: "proceed to section_problem_importance"
    - from: "need_intensity_competitors"
      to: "section_competitors_research"
      via: "stored variable reused as pre-seeded competitor list"
      pattern: "need_intensity_competitors"
---

<objective>
Add section_need_intensity to foundation-sprint.md, inserted immediately after section_problem locks and before section_problem_importance. The section runs the full Need Intensity scoring flow: dimension introduction, user self-rating, AI web search and calibration, formula computation with tier label, below-1000 advisory loop, and competitor data handoff.

Purpose: Implements requirements NEED-01 through NEED-06 — the core Need Intensity interaction that gates the rest of Step 1 with market validation signal.
Output: foundation-sprint.md with section_need_intensity inserted in the correct sequence, navigation_controls DISCARD RULE updated.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-english-need-intensity-workflow/13-CONTEXT.md
@get-your-shit-together/workflows/foundation-sprint.md

<interfaces>
<!-- Key structural patterns extracted from foundation-sprint.md for executor reference. -->

Section insertion point:
- section_problem ends at line ~252 with: "Then proceed to Section 3 (Founder Advantages). Do not ask anything else in this section."
- That line must be changed to: "Then proceed to section_need_intensity. Do not ask anything else in this section."
- section_need_intensity is inserted between section_problem (ends ~line 252) and section_problem_importance (starts ~line 254)
- section_problem_importance ends: "Then proceed to section_advantages. Do not ask anything else in this section." — unchanged

Non-blocking awareness pass pattern (used by section_purchaser, section_problem_importance, section_market_sizing):
- No lock announcement
- No Step 1 banner re-render
- One confirmatory question only OR structured multi-step flow
- NOTE: section_need_intensity is NOT a non-blocking pass — it has its own calibration exchange and advisory loop

Named field capture pattern (how scorecard fields are stored — follow exactly):
- **need_intensity_real** = "[calibrated score 0-10]"
- **need_intensity_urgent** = "[calibrated score 0-10]"
- **need_intensity_critical** = "[calibrated score 0-10]"
- **need_intensity_imposed** = "[calibrated score 0-10]"
- **need_intensity_neglected** = "[calibrated score 0-10]"
- **need_intensity_consciousness** = "[calibrated score 0-10]"
- **need_intensity_score** = "[computed formula result 0-6000]"
- **need_intensity_tier** = "[tier label from README]"
- **need_intensity_rationale** = "[AI rationale per dimension — stored for NEED-INTENSITY.md assembly]"
- **need_intensity_competitors** = "[competitor names found during web search — reused by section_competitors]"

Tier labels (canonical from README.md — use exactly):
- 4,000 – 6,000: "Burning need — strong market signal"
- 2,500 – 3,999: "Solid need — viable if execution is sharp"
- 1,000 – 2,499: "Moderate need — segment or reframe before building"
- 500 – 999: "Weak need — advisory: consider a tighter client definition"
- 0 – 499: "Minimal need — significant risk, revisit problem statement"

Below-1000 advisory loop (from CONTEXT.md locked decisions):
- AI suggests one tighter client segment AND one problem reframe (two distinct suggestions)
- On re-rate: AI does the full 6-dimension re-rating itself — user only picks the reframe
- Maximum 5 loops; after 5 loops AI recommends highest-scoring attempt with rationale
- Flow is advisory, never blocking — user can always proceed

Calibration rules (from CONTEXT.md locked decisions):
- Downward only — AI can only propose lower ratings, never higher
- No evidence for a dimension → keep user's original rating, say so explicitly
- Per-dimension: AI shows specific search finding AND reasoning, proposes revised score, asks if user agrees
- If user disagrees: user's original rating stands

Competitor data handoff:
- Store competitor names from Need Intensity web search as need_intensity_competitors
- section_competitors must reference need_intensity_competitors as pre-seeded data (no second search for competitors)
- The section_competitors_research brief must pass need_intensity_competitors to gyst-researcher as user_named_competitors equivalent

DISCARD RULE update in navigation_controls:
- "User goes back to Customer segment": also wipe all need_intensity_* fields
- "User goes back to Problem": also wipe all need_intensity_* fields
- New entry: "User goes back to Need Intensity": wipe all need_intensity_* fields only. Re-run section_need_intensity (Customer, Purchaser, Problem stay locked).
- Add "Need Intensity" to the list of sub-decisions in the revisit question
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert section_need_intensity into foundation-sprint.md</name>
  <files>get-your-shit-together/workflows/foundation-sprint.md</files>
  <action>
Insert a new `<section name="section_need_intensity">` block into foundation-sprint.md between section_problem and section_problem_importance. Also update the transition line at the end of section_problem and update navigation_controls.

**Step A — Update section_problem transition line:**
Find the final line of section_problem (currently reads "Then proceed to Section 3 (Founder Advantages). Do not ask anything else in this section.") and change it to:
"Then proceed to section_need_intensity. Do not ask anything else in this section."

**Step B — Insert section_need_intensity after the closing `</section>` of section_problem:**

```xml
<section name="section_need_intensity">

## Need Intensity Scoring (NEED-01 through NEED-06)

**When entering this section:** Customer and Problem are already locked. Do not re-ask for them.

---

### Step 1 — Introduce Need Intensity

Before showing the dimensions, present this introduction exactly:

"Before we map your advantages and competitors, let's score how intensely this market actually needs a solution. Need Intensity measures whether the pain is deep enough to drive purchasing behavior — not whether your idea is clever.

I'll show you 6 dimensions. Rate each one 0–10. After that, I'll run a web search to calibrate your scores."

---

### Step 2 — Show all 6 dimensions and collect user ratings

Present all 6 dimensions in a single block. The user rates all 6 in one response — not one at a time.

Use this format:

---

Rate each dimension 0–10. Both extremes are defined to anchor your rating. Reply with 6 numbers in order (e.g., "7, 4, 8, 3, 6, 5"):

**1. Real** — Is there documented evidence people are actively trying to solve this?
- 0 = No communities, no tools, no job postings — problem may not exist at scale
- 10 = Large communities, dedicated tools, active job market all confirm this problem

**2. Urgent** — Do people need a solution now, not eventually?
- 0 = "Would be nice someday" — no time pressure, low willingness to pay today
- 10 = People are losing money or missing deadlines right now — they will pay immediately

**3. Critical** — How severe is the consequence of NOT solving it?
- 0 = Minor inconvenience — forgotten by next week
- 10 = Loss of income, health risk, or regulatory exposure — catastrophic to ignore

**4. Imposed** — Is the need externally mandated (by law, employer, or market standard)?
- 0 = Entirely optional — buyer can choose to ignore it
- 10 = Legally required or employer-mandated — buyer has no choice

**5. Neglected** — How thin is the existing solution landscape?
- 0 = Crowded market with dominant, well-regarded tools — no whitespace
- 10 = No serious solutions exist — buyers are improvising with spreadsheets or nothing

**6. Consciousness** — Does the target customer already know they have this problem?
- 0 = Buyers don't recognize this as a distinct problem — you'd need to educate heavily
- 10 = Buyers actively search for solutions — they know exactly what they need

---

Wait for the user to respond with 6 ratings. Accept any readable format (comma-separated, numbered list, etc.). Parse the 6 values and store them as user_ni_real, user_ni_urgent, user_ni_critical, user_ni_imposed, user_ni_neglected, user_ni_consciousness.

---

### Step 3 — Web search and per-dimension calibration

Say:
"Got it. Searching now to calibrate your scores."

Run an inline WebSearch:
- Query 1: "[locked customer segment] [locked problem] solutions tools alternatives 2024 2025"
- Query 2: "[locked customer segment] [locked problem] community forum discussion dominant solution"

The goal: identify who is trying to solve this problem (feeds Real and Neglected scores) and whether any solution is well-regarded or dominant (feeds Neglected score). Store competitor names found as `need_intensity_competitors` — this list will be reused for COMPETITORS.md so the competitor search does not run again.

**After the search, calibrate each dimension in sequence. For each dimension:**

Show the specific evidence you found, propose a revised score (downward only — never higher than the user's rating), and ask if the user agrees. Use this pattern:

For Real: "I [found / didn't find] [specific evidence — e.g., 'a 45,000-member subreddit and 3 dedicated tools']. Based on this, I'd rate Real at [proposed score], [lower than / the same as] your [user score]. [Agree, or does your inside knowledge suggest otherwise?]"

For Urgent: "The evidence [shows / doesn't show] [urgency signal — e.g., 'time-sensitive job postings and SLA requirements']. I'd rate Urgent at [proposed score]. [Agree?]"

...and so on for each dimension.

Rules:
- If you found no strong evidence for a dimension: keep the user's original rating and say: "I didn't find strong signals on [Dimension] — keeping your rating of [score]."
- If the user disagrees with your proposed score: take their original rating. Do not push back.
- Store final calibrated score (user's or AI's, whichever was accepted) for each dimension.

Store named fields:
- **need_intensity_real** = "[final calibrated score 0-10]"
- **need_intensity_urgent** = "[final calibrated score 0-10]"
- **need_intensity_critical** = "[final calibrated score 0-10]"
- **need_intensity_imposed** = "[final calibrated score 0-10]"
- **need_intensity_neglected** = "[final calibrated score 0-10]"
- **need_intensity_consciousness** = "[final calibrated score 0-10]"
- **need_intensity_rationale** = "[per-dimension reasoning summary: what you found and why you proposed the score you did — 1-2 sentences per dimension, stored for NEED-INTENSITY.md assembly]"
- **need_intensity_competitors** = "[list of competitor/solution names found during web search — stored for reuse in section_competitors_research]"

---

### Step 4 — Compute formula and display result

After all 6 scores are calibrated and accepted:

Compute:
Score = need_intensity_neglected × (need_intensity_critical + need_intensity_consciousness) × (need_intensity_urgent + need_intensity_imposed + need_intensity_real)

Display the calculation explicitly:

"Need Intensity score:

Neglected ([score]) × (Critical ([score]) + Consciousness ([score])) × (Urgent ([score]) + Imposed ([score]) + Real ([score]))
= [neglected] × [critical + consciousness] × [urgent + imposed + real]
= **[final score]** / 6,000

Verdict: **[tier label]**"

Use the exact tier labels:
- 4,000–6,000: "Burning need — strong market signal"
- 2,500–3,999: "Solid need — viable if execution is sharp"
- 1,000–2,499: "Moderate need — segment or reframe before building"
- 500–999: "Weak need — advisory: consider a tighter client definition"
- 0–499: "Minimal need — significant risk, revisit problem statement"

Store:
- **need_intensity_score** = "[computed score]"
- **need_intensity_tier** = "[exact tier label]"

---

### Step 5 — Below-1000 advisory loop (conditional)

**Only run this if need_intensity_score < 1,000. If score ≥ 1,000, skip directly to Step 6.**

Initialize: loop_count = 0, best_score = need_intensity_score, best_attempt = [current framing: customer + problem + all 6 scores]

**Advisory loop (repeat up to 5 times while score < 1,000 AND loop_count < 5):**

loop_count = loop_count + 1

Say:
"Your score of [score] is below 1,000 — this suggests the need may not be strong enough to drive reliable purchasing. Here are two directions that could improve it:

**Tighter client segment:** [specific narrower segment suggestion — e.g., 'Instead of "solo founders", consider "solo founders building B2B SaaS with ≥1 paying customer"']

**Problem reframe:** [specific reframe suggestion — e.g., 'Instead of "onboarding complexity", consider "churn in the first 30 days caused by failed onboarding" — more urgent and critical framing']

This is advisory — you can re-rate with one of these directions, or proceed as-is. Which would you prefer?

**A)** Re-rate with tighter client segment
**B)** Re-rate with problem reframe
**C)** Proceed anyway with current score of [score]"

Wait for user response. If C: exit the loop and proceed to Step 6 with current scores.

**If user picks A or B (re-rate):**
- Apply the chosen reframe to the client+problem framing
- Re-run the full 6-dimension scoring (AI rates all 6 dimensions directly based on the reframed context — user does NOT re-rate; user only chose the direction)
- Recompute the formula
- Display the new score and tier
- If new score > best_score: update best_score and best_attempt
- If new score ≥ 1,000: exit loop, proceed to Step 6 with new scores
- If new score < 1,000 AND loop_count < 5: repeat loop

**After 5 loops without crossing 1,000:**

Display:
"After [5] reframe attempts, your highest-scoring framing was [best_attempt framing] at [best_score]. Here's why it's worth considering as your starting point: [1-2 sentence rationale for why that framing has the most signal].

You can proceed with this framing, or continue the sprint with your original framing. Your call — this advisory loop never blocks progress."

Wait for user response. Accept their decision. Then proceed to Step 6 with whichever scores the user chose.

---

### Step 6 — Transition

After Step 4 (or after the advisory loop if it ran):

Do NOT re-render the Step 1 banner (Need Intensity is not a banner field).

Then proceed to section_problem_importance. Do not ask anything else in this section.

</section>
```

**Step C — Update navigation_controls DISCARD RULE:**

1. In the revisit question list, add "Need Intensity" between "Problem" and "Problem I/U classification":
   Change: "(Customer segment / Purchaser / Problem / Problem I/U classification / Founder advantages / Competitors / Market sizing)"
   To: "(Customer segment / Purchaser / Problem / Need Intensity / Problem I/U classification / Founder advantages / Competitors / Market sizing)"

2. In the "User goes back to Customer segment" discard example, add `need_intensity_*` to the fields wiped:
   Change: "wipe scorecard_purchaser_*, scorecard_problem_iu, scorecard_problem_iu_nudge, Problem, Advantages, Competitors, scorecard_market_*"
   To: "wipe scorecard_purchaser_*, scorecard_problem_iu, scorecard_problem_iu_nudge, need_intensity_*, Problem, Advantages, Competitors, scorecard_market_*"

3. In the "User goes back to Problem" discard example, add `need_intensity_*`:
   Change: "wipe scorecard_problem_iu, scorecard_problem_iu_nudge, Advantages, Competitors, scorecard_market_*"
   To: "wipe need_intensity_*, scorecard_problem_iu, scorecard_problem_iu_nudge, Advantages, Competitors, scorecard_market_*"

4. Add a new discard entry after the Problem entry:
   "User goes back to **Need Intensity**: wipe need_intensity_* only. Re-run section_need_intensity only (Customer, Purchaser, Problem stay locked)."

5. In the "User goes back to Founder advantages" entry, add `need_intensity_*` note is NOT wiped (Need Intensity is upstream of Advantages, so Advantages rollback does not affect it — leave as-is).

6. In the "Start Step 1 over" wipe list, add `need_intensity_*`:
   Change: "customer segment, purchaser (scorecard_purchaser_*), problem, problem I/U classification (scorecard_problem_iu, scorecard_problem_iu_nudge), advantages, competitors, market sizing (scorecard_market_*)"
   To: "customer segment, purchaser (scorecard_purchaser_*), problem, need intensity (need_intensity_*), problem I/U classification (scorecard_problem_iu, scorecard_problem_iu_nudge), advantages, competitors, market sizing (scorecard_market_*)"

**Step D — Update section_competitors_research to reuse need_intensity_competitors:**

Find the brief template in section_competitors_research (the block passed to gyst-researcher). Currently:
```
Customer segment: [locked customer segment from section_customer]
Problem: [locked problem from section_problem]
User-named competitors: [what the user said in section_competitors, or "none"]
```

Update to:
```
Customer segment: [locked customer segment from section_customer]
Problem: [locked problem from section_problem]
User-named competitors: [what the user said in section_competitors, or "none"]
Pre-identified solutions: [need_intensity_competitors — names found during Need Intensity web search; include these in the competitor list without re-searching for them]
```

Also add a note before the brief invocation:
"IMPORTANT: need_intensity_competitors are already known from the Need Intensity web search. Pass them to gyst-researcher as pre-identified solutions so the researcher does not spend search capacity finding them again."
  </action>
  <verify>
    <automated>grep -n "section_need_intensity\|need_intensity_competitors\|need_intensity_score\|need_intensity_tier\|need_intensity_rationale\|need_intensity_neglected\|need_intensity_real\|below 1,000\|loop_count\|Burning need\|Solid need\|Moderate need\|Weak need\|Minimal need" C:/Users/raphg/Desktop/GYST/get-your-shit-together/workflows/foundation-sprint.md | head -40</automated>
  </verify>
  <done>
    - `section name="section_need_intensity"` exists in foundation-sprint.md between section_problem and section_problem_importance
    - section_problem now transitions to section_need_intensity (not directly to section_advantages)
    - section_need_intensity transitions to section_problem_importance at Step 6
    - All 5 tier labels (Burning need, Solid need, Moderate need, Weak need, Minimal need) are present
    - need_intensity_competitors named field is captured and passed to section_competitors_research brief
    - navigation_controls DISCARD RULE includes "Need Intensity" as a revisit option with correct wipe scope
    - All 10 named need_intensity_* fields are defined in the section
  </done>
</task>

</tasks>

<verification>
Run the following to confirm all insertion points and named fields are present:

```bash
grep -c "section_need_intensity" get-your-shit-together/workflows/foundation-sprint.md
# Expected: at least 3 (section open tag, transition from section_problem, transition to section_problem_importance)

grep -n "need_intensity_" get-your-shit-together/workflows/foundation-sprint.md | wc -l
# Expected: at least 15 (10 named fields + references in DISCARD RULE + competitors brief)

grep -n "Burning need\|Solid need\|Moderate need\|Weak need\|Minimal need" get-your-shit-together/workflows/foundation-sprint.md
# Expected: 5 lines, all in section_need_intensity

grep -n "Need Intensity" get-your-shit-together/workflows/navigation_controls 2>/dev/null || grep -n "Need Intensity" get-your-shit-together/workflows/foundation-sprint.md
# Expected: appears in DISCARD RULE revisit question
```
</verification>

<success_criteria>
- section_need_intensity is present in foundation-sprint.md at the correct position (after section_problem, before section_problem_importance)
- The 6-dimension rating block shows all 6 dimensions with 0-10 anchors in one user response
- Web search calibration is downward-only with per-dimension evidence disclosure
- Formula is computed and displayed with the exact tier label from README.md
- Below-1000 advisory loop: max 5 loops, AI re-rates (not user), never blocking
- need_intensity_competitors is stored and passed to section_competitors_research — no duplicate search
- navigation_controls includes "Need Intensity" as a revisit option with correct discard scope
- NEED-01, NEED-02, NEED-03, NEED-04, NEED-05, NEED-06 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/13-english-need-intensity-workflow/13-01-SUMMARY.md`
</output>
